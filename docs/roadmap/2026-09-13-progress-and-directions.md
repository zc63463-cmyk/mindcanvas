# mindcanvas 进度深度自评与后续方向（2026-09-13）

> 取证时点：HEAD `0f46da6`（1.8.12 收口）· 三包 480/1127/186 = **1793 全绿** · lint 1468+46 · 远端 `github.com/zc63463-cmyk/mdcanvas`（187 commits）
> 本文取代 `2026-08-28-progress-audit.md` 作为**当前进度基线**；08-30 三步走计划（`2026-08-30-next-phase-roadmap.md`）保留为对照物。

## 〇、一句话结论

**引擎与画布工程已「超额完成」，产品化大部落地，唯一卡口仍是价值线（B）**；升中心方向的"未深入"不是能力问题，而是**语义收口 + 守卫 + 一次 UX 决策**（见 §四）。

## 一、总体进度（加权自评）

| 维度 | 权重 | 完成度 | 依据（可核对） |
|---|---|---|---|
| 内核/画布工程 | 30% | **100%（超额）** | 1.0.0 冻结后 12 个 minor；测试 395→1793；测试比 0.82；门禁 8 脚本 + ADR×9 |
| 画布产品化 | 25% | **~75%** | 启动页/文件管理/目录工作区/句柄持久化/自动保存/**PWA** 已落地；统一侧栏、错误边界未见 |
| 价值线 B | 35% | **~8%** | B0 实测已跑 + D1 材料在手；B1–B5 **零实现** |
| 生态/部署 | 10% | **~5%** | I3 刻意后置；E1/E2 门槛（B3）未达 |

> 加权 ≈ 0.30×100 + 0.25×75 + 0.35×8 + 0.10×5 ≈ **52%**（08-28 审计口径约 40%）。

## 二、进度台账

| 指标 | 08-28 审计 | 现在 | 倍数 |
|---|---|---|---|
| 测试用例 | 395（kernel 266 + react 129） | **1793**（480 / 1127 / 186） | 4.5× |
| 生产代码 | 7,517 行（kernel+react src） | **38,377 行**（三包 src 220 文件） | 5.1× |
| 测试代码 | 5,196 行（比 0.69） | **31,283 行（比 0.82）** | — |
| commits | 64 | **187**（远端已建） | 2.9× |
| 文档 | 33 份 / 4 ADR | dispatch 49 份 / ADR 9 条 / outputs 20+ | — |

**这两周半的版本主线**（`CHANGELOG.md`）：边一等公民（1.1–1.2）→ 描述与序列化保真（1.3）→ 注释双区域 + **布局岛/cid/`note.dir`（升中心地基）**（1.4）→ Section Phase 1（1.5）→ 出线长度（1.6）→ 四向分叉/枢纽/梁（1.7）→ 环形菜单 Phase 1（1.8.0）→ 性能地基 + 路由治理（1.8.2–1.8.3）→ **关系线 R0–R5**（1.8.4–1.8.7）→ 编辑流/导航/渲染一致性（1.8.8–1.8.11）→ 边会话深度修复（1.8.12）。

## 三、08-30「三步走」执行审计（本次自评的核心）

| 计划项 | 状态 | 证据 |
|---|---|---|
| D-1 I1 远端推送 | ✅ **已完成** | `git remote` = …mdcanvas；187 commits（审计时 136、零远端） |
| D-2 B0 实测 | ✅ **已完成（08-30 当天）** | `llm/out/b0-quality-report.md`：gateway 15 节点/1 提议、roadmap 11/4、ideas-pool 7/0；语义 **5/5 方向正确**，结构 3/5（2 个 E 全因 `skip` 的 schema 歧义）→ **建议 D1=达标（带 1 轮 prompt 修正）** |
| S 产品壳 | 🟡 **部分** | S3 PWA ✅（`apps/canvas/public/manifest.webmanifest` + `sw.js` + `index.html` 引用）；启动页/文件管理器/工作区/autosave ✅；**S1 统一侧栏/抽屉、S4 错误边界未见落档** |
| F 自用验证周 | ❌ **产出缺失** | `docs/work/` 不存在、**无摩擦清单**落档、`dogfood-output/` 仅一份 8/29 画布 → 08-30 指定的"下一轮功能规划唯一事实源"没有留下 |
| B 线回归（B1–B5） | ❌ **未启动** | CHANGELOG 1.8.x 无 Sidecar/对账/MCP 痕迹 |

**偏差诊断**：08-30 写"❌ 继续无缺陷清单驱动的功能批次"，而 09-04→09-13 全是功能批次。**不是失控**——每批都是用户可见的编辑体验缺口，且执行质量极高（派遣→TDD 先红→独立复核→阴性对照）；**问题是"需求来源无约束"，价值验证被持续推后**。

**B0 的两处方法论缺口**（`b0-quality-report.md:32-35` 自述）：
1. 执行者是**有项目上下文的 LLM** → 语义可能被高估，建议独立环境对照；
2. 样本仅 3 张演示画布 → 真实大画布（50+ 节点）质量未测。

## 四、升中心专项：现状深度与六条"未深入"

**已做深**（全链有测试）：协议 `note.centers`/`center_pos`（`docs/specs/2026-09-02-mm-md-protocol.md:306` §6.3）→ cid 单调身份 + 锚迁移 cid 双轨（`packages/kernel/src/registry/anchor-migrate.ts:290`）→ 岛投影递归剪枝 + `layoutForest`（`packages/kernel/src/layout/islands.ts:184-214`）→ 拖动整岛偏移预览（`MapView.tsx:762-780`）+ 单条 undo 提交（`MindmapStage.tsx:891-909`）→ 右键四向升格/降格/接回/父级连接（`nodeMenuBags.ts:47-116`、`contextMenuItems.ts:256-335`）+ 环第二席（`subRingItems.ts:86-88`）→ Section⇒center（D1）与 detached 语义（`cutAttach.ts`）→ 边界补线 `boundaryLinks` + 导出随附。

**六条"未深入"**（逐条带出处）：

| # | 缺口 | 出处 |
|---|---|---|
| 1 | **升格写路径双轨**：右键/环走 `upsertCenter`（**不分配 cid**）；只有「设为 Section（升为中心）」走 `planPromoteCenter`（分配 cid）→ 同一动作两种身份语义 | `nodeMenuBags.ts:90-95` vs `centers.ts:429-462`、`MindmapStage.tsx:1012-1030` |
| 2 | 嵌套/跨岛移动语义"**设计确认中**"（当前行为经取证 = 子岛成员**不跟随**父中心，但从未被钉住） | `outputs/2026-09-10-sections-phase1-delivery.md:132`；`pipeline.ts:270-274` |
| 3 | 工程守卫薄：`planPromoteCenter` **无直接单测**；`onPromote`/`onDemote`/`onToggleParentLink` 写路径无直测 | 全仓检索（只有 `center-actions-attach-notice.test.tsx` 覆盖 onAttach 诊断） |
| 4 | **中心无任何视觉标记**（NodeG 无 center 语义；框只有 Section）→ "升了中心看不出来" | `NodeG.tsx` 全文件无 center 相关；`SectionLayer` 只画 Section |
| 5 | 森林布局**无缓存**（多中心大文档全量重算） | `islands.ts:18`、`outputs/2026-09-05-layout-islands-a6-acceptance.md:64`、`outputs/architecture-godot-decision-2026-09-05.md:94`（P2 待实测） |
| 6 | 协议 Phase 2 字段未落：`members` 管道串 / `collapsed` 落盘 | `outputs/2026-09-10-sections-phase1-delivery.md:128-132`、`docs/specs/2026-09-02-mm-md-protocol.md:347-359` |

**边界（已知不做）**：detached 中心无"画布占位/收件箱"（裁决走面板可发现性，`CHANGELOG.md:103`）；Canvas 模式不渲染 Section/boundaryLinks（A6 门禁强制 SVG + R5-3 降级提示兜底）。

→ 建议的任务包与派遣稿：`docs/dispatch/2026-09-13-centers-phase2-plan.md`（C1 写路径统一 / C2 守卫 / C3 嵌套语义收口 / C4 可见性 / C5 缓存取证）。

## 五、后续方向与行动指导

### P0 · 本周（先做掉"没有它后面全是臆测"的）

1. **F 周补做（最小版）**：自举三张真实画布（本项目 roadmap / 手头迁移任务 / 技能路线）→ 日常推进一天 → **摩擦清单落档**（`docs/work/` + 清单文件）。它是 08-30 指定的"下一轮功能规划唯一事实源"，现在是空的。
2. **B0 方法论补强**：① 独立环境 LLM（干净会话）复跑；② 50+ 节点真实大画布复测 → 给 D1 一个**可辩护的定论**（达成 → B1 立项；不达成 → prompt 迭代 ≤2 轮，再不行回 spec）。
3. **卫生批（半天）**：根目录 **145 个 `.log/.png`** 清收（`gitignore` 但污染工作区）；`docs/2026-09-08-graph-engine-fix-list.md`（**0 字节**）删或补；三个历史升格 bug 的修复补记 commit 来源（现只在 `outputs/2026-09-10-sections-phase1-delivery.md:53-57` 有结论、查不到提交）。

### P1 · 下一批（二选一或并行）

- **A. B1 最小纵切**（2–3 天时间盒）：只读版 Sidecar——读 `.mm.md` → 出提议集 → 画布旁只读面板 → 你点一次"接受"写一条 issue。**判据 = 端到端跑通一次真实提议**。这是把"B0 有材料"变成"B 线有代码"的最小步骤。
- **B. 升中心 Phase 2**（与 A 可并行）：见派遣稿（C1–C4 约 1–2 天 + C5 半天取证）。

### P2 · 之后

产品壳收口（S1 统一侧栏/抽屉 + S4 错误边界）→ Canvas 大图专项（Section/中心/自由边的 Canvas 渲染）→ 移动端触控深化 → I3 部署。

### 建议恢复的一条纪律（比任何单个功能值钱）

**引擎侧新功能批次必须有一条"摩擦清单条目"背书**（恢复 08-30 的「❌ 无缺陷清单驱动的功能批次」）。这两周证明了执行能力，缺的只是需求来源的约束。

## 六、风险台账（承接 08-28，更新）

| # | 等级 | 项 | 现状 |
|---|---|---|---|
| R1 | **高（未消解）** | 核心价值假设（LLM 提议质量） | B0 已跑但**方法论有缺口**（同源 LLM + 小样本）→ P0-2 |
| R2 | 中 | B5 画布集成是最重单块 | 未动；规划已预判拆两批 |
| R3 | ~~中~~ | 版本安全网缺失 | **已消除**（远端已建，187 commits） |
| R4 | 中 | **需求来源无约束**（新） | 12 个 minor 无摩擦清单背书 → P0-1 + 纪律恢复 |
| R5 | 低 | 快速注释两次重做的经验 | 已沉淀 |
| R6 | 低 | 根目录 145 个日志/截图 | P0-3 卫生批 |
| R7 | 低 | `docs/.../graph-engine-fix-list.md` 空文件 | P0-3 |
