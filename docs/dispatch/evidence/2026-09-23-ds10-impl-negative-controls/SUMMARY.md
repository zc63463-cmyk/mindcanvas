# DS-10 实施批 · 判别性负控实测汇总（机器可核）

- 日期：2026-09-23
- 执行者：DS-10 实施批施工包
- 被测对象：`apps/canvas/src/docIndexProject.ts` 的 (b') 删除判据与 F3 排除
  （实现提交 `db9ac72`；用例提交 `a083730`）
- 方法：`/tmp/ds10-nc/nc.sh` 六要素流程
  （中性化 → **落盘 grep 计数校验** → 跑测并单独捕获退出码 → 转红原文 →
  备份还原 + `cmp` 字节校验 → 回绿读数）
- 还原方式：备份 + `cmp`（**不用** `git checkout` —— 工作树 CRLF 会被改写 EOL）
- 还原 sha256：五条中性化前后**同一文件同一值**
  `dea33fb41b1f4825e187e211bee780afed7272b880df660f597af54aea6a57fb`

本目录每条的 `.log`（转红态）与 `-restore.log`（回绿态）是**原始输出**。

## 一、六要素逐条

| # | 编号 | 中性化了什么（落盘变异） | 落盘 grep 计数校验 | 转红用例（用例名 + 断言行） | 转红 expected / received | 转红退出码 | 回绿读数 | 还原 sha256 |
|---|---|---|---|---|---|---|---|---|
| 1 | NC1 | (b') 的认领条件恒假（等价于「从不回收」）→ 被取代旧行重现 | `NEUTRALIZED(NC1)` ×1 | `不变量 5：改名后旧行被回收…` @ `:1695`（同轮 `:1716` / `:1750` / `:1817` / `:1834`） | expected `['新名.mm.md']` / received `['新名.mm.md','旧名.mm.md']` | **1** | 75 passed | `dea33fb4…` |
| 2 | NC2 | 去掉「已被认领」条件（只要求非当前 id 即回收）→ 判据扩到孤儿行 | `NEUTRALIZED(NC2)` ×1 | `不变量 1：孤儿行（从未被认领）一律保留` @ `:1715`（同轮 `:1729` / `:1752` / `:1834`；既有 `投影保 source…` @ `:828`） | expected 含 `孤儿.mm.md` / received 丢失（`:828` expected 12 / received 11） | **1** | 75 passed | `dea33fb4…` |
| 3 | NC3 | 畸形行不再附着（判据从「可解析」扩到「全部旧行」） | `NEUTRALIZED(NC3)` ×1 | `不变量 2：畸形行保留…` @ `:1775`（同轮既有 `:630` / `:280` / `:1066`） | `:1775` expected `kept` 长 3 / received 少行（`:280` expected 2 / received 0） | **1** | 75 passed | `dea33fb4…` |
| 4 | NC4 | 去掉 F3 partial 排除（`!sourceStillPresent` → `true`）→ 未消解源行被删 | `NEUTRALIZED(NC4)` ×1 | `★不变量 6（F3-部分成功）：未消解的源行保留…` @ `:1850` | expected 含 `源.mm.md` / received `['目标.mm.md','孤儿.mm.md']` | **1** | 75 passed | `dea33fb4…` |
| 5 | NC5 | 每次投影重放一遍原行 → 行集不幂等 | `NEUTRALIZED(NC5)` ×1 | `不变量 4：幂等——投影连续两次行集一致…` @ `:1750`（同轮 `:1695` / `:1716` / `:1817` / `:1834` / `:469` / `:828`，共 7 条） | expected 行集一致 / received 多一行（`:1695` expected `['新名.mm.md']` / received `['新名.mm.md','新名.mm.md']`） | **1** | 75 passed | `dea33fb4…` |

五条**全部实际转红**（`code=1`），且每条都能追溯到**它所针对的那一条不变量**；
回绿态一律 `75 passed`（67 既有 + 8 新增，零回归）。

## 二、转红读数（各条内部计数）

| 编号 | 转红读数 | 回绿读数 |
|---|---|---|
| NC1 | 5 failed / 70 passed (75) | 75 passed |
| NC2 | 5 failed / 70 passed (75) | 75 passed |
| NC3 | 4 failed / 71 passed (75) | 75 passed |
| NC4 | 1 failed / 74 passed (75) | 75 passed |
| NC5 | 7 failed / 68 passed (75) | 75 passed |

## 三、判别力的方向性说明（NC4 为何只有 1 条）

NC4 刻意**只**去掉 F3 排除、其余判据不动，所以只有专测该不变量的那一条转红
（1 failed）。这是**期望形状**，不是判别力不足：它在断言 F3 源行"必须被跳过"，
中性化后该行被回收 → 恰好这一条红。反过来，NC1/NC2/NC5 影响面更宽
（分别关掉全部回收、放宽到孤儿行、破坏幂等），故红面更大——
红面大小与被针对的判据宽度一致。

## 四、纪律声明

- 未使用 `it.fails` / 反转断言 / `catch` 后报 PASS / 宽泛 `skip` / 删除或弱化既有断言；
- 每条中性化都**先在盘上用 grep 计数确认**（见「落盘 grep 计数校验」列），
  未落盘的变异不计为「已证明转红」；
- 退出码单独一行捕获（`code=$?`，不与命令替换混写 —— 本机 bash 3.2 会污染 `$?`）；
- 还原用备份 + `cmp` 字节校验，并记录还原后 sha256 与回绿 passed 数；
  五条的 sha256 一致，且 `git diff HEAD -- apps/canvas/src apps/canvas/tests` 为空
  （证明中性化未以任何形式残留）。
