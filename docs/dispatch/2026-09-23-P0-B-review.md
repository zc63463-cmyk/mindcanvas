# P0-B 独立审查报告（只读复核）

- 审查对象：`docs/dispatch/2026-09-23-P0-B-report.md`（P0-B 资产上传：三态、同名与归一化）
- 审查日期：2026-09-23
- 审查方式：**只读独立复核**（未修改任何施工产物；唯一写入是本报告与 /tmp 下的复核日志；负控复现全程备份 + cmp 还原，收工时工作树 `git status --porcelain` 为空）
- 仓库：`mindcanvas-s5`（main @ `0a09216`）
- 复核证据：`/tmp/p0b-review/`（gate:fast / build / react / canvas 全量日志 + A1-LATE 负控复现日志）

---

## 0. 结论

**ACCEPT —— P0-B 按其交付范围通过。**

判定依据：基准锚定三重核实（开工锚逐字一致 / 线性血缘 / 产品树锚变化全部来自本包）；门禁与测试计数**全量复跑且与回执逐字一致**；8 条负控证据日志读数全部对上、6 个还原 sha256 与当前工作树**逐字节一致**、A1-LATE 由本审查**独立复现**（中性化 → 转红 exit 1 → cmp 还原 → 回绿 → 工作树干净）；mode-guard 断言未改（diff 仅 `0→20ms` + 注释）；勘误 E-1/E-2/E-3 全部落实且 E-3 实证机制属实；`AssetHost` 既有签名零删除（纯增量 `AssetHostV2`）；`FileManager.tsx` 未触碰；停止点守住（无删除入口）。

**边界（与回执 §11 一致，本审查维持）**：A1/A3 人工项与 N1/N4 真实「保存→关闭→重开」旅程维持 unconfirmed，本报告不构成 `acceptance-and-backlog.md` §6 收口判据已满足的证据。

发现一项中等与四项低级别问题（§5），均不阻塞验收。

---

## 1. 基准锚定（全部实测）

| 核对项 | 回执声明 | 实测 | 结果 |
|---|---|---|---|
| 开工 HEAD | `0d54575` | `0d54575a93c5…` | ✅ |
| 开工 `HEAD:packages` | `27b2909c…`（与派单书期望逐字一致） | `27b2909c1ebe9bcf049d3998e52a56109d26b0ac` | ✅ 逐字一致 |
| 开工 `HEAD:apps` | `3cd755d2…` | `3cd755d212923f409e44a20d855fd02b572e2c66` | ✅ 逐字一致 |
| 收工 `HEAD:packages` | `da74e4de…` | `da74e4dee4f806a0f270ce955221530ec257b6aa` | ✅ |
| 收工 `HEAD:apps` | `3c47bfd4…` | `3c47bfd41c0a3797064105cc72e0facc8ebf23f4` | ✅ |
| 线性血缘 | `merge-base --is-ancestor 0d54575 HEAD` | 成立，无 rebase/改写 | ✅ |
| 工作树 | 干净 | `git status --porcelain` 为空 | ✅ |
| 分支 | main | main | ✅ |

期望锚 `27b2909c` / `3cd755d2` 与 P0-A 回执（`2026-09-23-P0-A-report.md` :31）记录的 P0-A 收工锚一致，交叉验证成立。**但 P0-B 派单书本体未入库**（见发现 M-1），「派单书 §一」目前只能靠 P0-A 回执交叉印证。

## 2. 门禁全量复跑（与回执逐字对照）

| 门禁 | 回执声明 | 本审查复跑 | 结果 |
|---|---|---|---|
| `pnpm gate:fast` | exit 0；lint 1533/48，608 files | exit 0；1533 warnings / 48 infos / 608 files | ✅ 逐字一致 |
| `pnpm build` | exit 0 | exit 0 | ✅ |
| budget 八项 | 持平，bang 89/90 | 八项全部持平，bang 89/90 ↓1 | ✅ |
| `packages/react` | 162 files / 1748 passed | 162 files / **1748 passed** | ✅ |
| `apps/canvas` | 56 files / 625 passed | 56 files / **625 passed** | ✅ |
| `doc-index.test.ts` | 75 | 75 passed | ✅ |

lint 较基线 1536 → 1533（下降 3）属实；`gate:fast` 内含 typecheck + depcruise，exit 0 一并覆盖。

## 3. 负控证据（8 条全部核验 + 1 条独立复现）

- **读数**：8 条 `.log` 的转红读数（含退出码 1）与 `-restore.log` 回绿读数逐条与 SUMMARY.md 一致（A1-LATE 1/32、A1-CACHEKEY 2/31、A2 4/29、A3-ITEM-ONLY 1/14、A3-SESSION 1/19、SP1 1/32、SP2 2/6、SP3 1/19，回绿 33/33/33/15/20/33/8/20）。
- **还原 sha256**：SUMMARY 表中 6 个还原哈希与当前工作树对应文件**逐字节一致**（assetHost `8f3b331c`、workspaceAssetHost `5dd03724`、idbAssetHost `d461b20f`、assetStoreCopy `d8f27b35`、assetWriteLedger `4aacc33d`、SidePanels `60efac4d`）。
- **独立复现（A1-LATE）**：本审查用施工方同一 patch 脚本（`/tmp/p0b-nc/p_a1_late.py`，锚点须唯一匹配否则退出）亲跑六要素：落盘标记 ×1 → 转红 exit 1（1 failed / 32 passed，与归档日志逐字一致）→ 备份 + cmp 还原 OK → 回绿 33 passed → 工作树干净。**判别力成立。**
- **patch 脚本抽查**：A1-LATE / A2 / SP3 的中性化内容与 SUMMARY 声明逐字吻合（A2 确实删除整个三选分支、SP3 确实让 child 直写 `item.id`）。
- **两轮假证据处置**：SUMMARY 记录的「前 6 条假红（cwd 用错）」与「SP3 第一轮不转红 → 补渲染层用例」与提交历史（`c55d956` SP3 判别力补强）交叉印证一致。SP3 渲染层用例真实存在（`asset-insert-normalize.test.tsx:444`，驱动真实 `SidePanels`）。

## 4. 代码与文档抽查

| 声明 | 实测 | 结果 |
|---|---|---|
| `assetHost.ts` 引用（:46 三选默认、:134-146 三态/三轴、:202 五态、:234 `cacheKeyOf`、:273 `AssetHostV2`） | 全部命中 | ✅ |
| `AssetHost` 既有签名未改 | 基线→HEAD diff **零删除行**，纯增量 | ✅ |
| `FileManager.tsx` 未触碰 | 变更文件清单无此文件 | ✅ |
| mode-guard 断言一字未改 | diff 仅 `setTimeout(resolve, 0)` → `20` + 注释；rAF 桩 `setTimeout(cb,16)` 声明属实 | ✅ |
| E-1/E-2/E-3 勘误落实 | `AssetWriteEntry` 改名（§4.5.4 :712/:727）+ `AssetIndexEntry` 补登（§4.7 :781）；kind ∈ {'img','draw'}（§6.1 :919、M7 :935）；§4.7.1 :794 + 预裁决差异并列 :836-839 | ✅ |
| 勘误只动一个文件 | `e597a90` = shared-contracts.md，+110/-3 | ✅ |
| E-3 实证机制 | `docIndexMigrate.ts` 确证：selfContained → `scopeId: BROWSER_SCOPE_ID`（:324）且 `relPath = null`（:307）；`parseAssetFavKey` :479-489 精确命中 | ✅（行号见 L-1） |
| 逐文件测试计数 | 基线 11/9/6/8/19/7（=60）→ 现 33/15/20/20/29/15（=132），+72 与包总数增量 1676→1748 精确吻合 | ✅（基线数由本审查从 `0d54575` 独立重数） |
| 停止点：无删除入口 | 全部 diff 无删除相关新增；`AssetHostV2` 无 delete/remove 方法 | ✅ |
| `resolveRefId` 三语义共用（SidePanels.tsx:153） | 定义 :153；icon/media/child 经 :207 同一调用 + 老路径 :240，与代码注释一致 | ✅ |
| MindmapStage 引用（:1145 `storeOf`、:1289 `uploadAssetDetailed`、:1331 `confirmDiscovered`、:2976 `normalizeInsert`） | 全部命中 | ✅ |

## 5. 发现（分级）

| # | 发现 | 级别 | 处置建议 |
|---|---|---|---|
| M-1 | **P0-B 派单书未入库**。P0-A 有独立派单书文件（`2026-09-23-P0-A-dispatch.md`），P0-B 的派单书只在回执中被引用（「派单书 §一」的期望锚、基线读数）而本体不在 `docs/dispatch/`。期望锚虽可经 P0-A 回执交叉验证（27b2909c/3cd755d2 = P0-A 收工锚），但审计链断裂：后续无法从仓库内独立核验「派单书期望值」与「开工基线复跑读数」。与摘要中「派单书路径写成 temp/workbase（实际 temp workbase）」同源——外发模板自身有缺陷且未留档 | **medium** | 派单书补入库（历史文件按纪律不改写，可作附录归档）；下批派单书模板固化「派单书必须随开工提交入库」纪律 |
| L-1 | E-3 引用行号偏移：回执与 shared-contracts.md:839 引 `docIndexMigrate.ts:295,325-326`，实测 `BROWSER_SCOPE_ID` 兜底在 :290/:324、`relPath = null` 在 :307、`assetKey: assetId` 在 :323。偏移 1~5 行，机制与结论不受影响 | low | 无需改历史文件；下批引用行号以 grep 实测为准 |
| L-2 | 规格 §4.1 给 P0-B 认领 **3** 个新增测试文件（含 `asset-scope-key.test.ts`），实际新增 2 个；`AssetKey` 纯函数测试折进扩展的 `asset-host.test.ts`（:126-129 有专门 describe 块）。规格 §4.1 与 §5.2 自身不一致（§5.2 只列 2 个），覆盖等价，但回执未显式说明此偏差 | low | 回执可补一句说明；或在 P1 前的规格维护中修正 §4.1 |
| L-3 | 「本包 7 个提交」口径滞后：回执提交（`0a1ae8d`）后还有收工锚更新提交 `0a09216`，`0d54575..HEAD` 实为 8 个；产品树锚的实际变化来自其中 5 个动 `packages/`+`apps/` 的提交。`0a09216` 的 commit message 已自我说明（DS-10 同款），血缘透明，不构成实质问题 | low | 无需处置；口径以「产品树锚 + 血缘」为准（回执已声明同款原则） |
| L-4 | 负控六要素之②「落盘 grep 计数」的原始 stdout 未归档进 evidence 目录（`.log` 仅含 vitest 输出），该环节目前靠 SUMMARY 转述 + `/tmp/p0b-nc/nc.sh` 脚本存在佐证。本审查以 A1-LATE 独立复现补上了这一环的验证 | low | 后续负控把 `nc.sh` 全程 stdout（含落盘计数行）一并归档为 `run-<tag>.log` |

## 6. 复核声明

- 本审查未修改任何施工产物、未修复任何缺陷、未删除任何证据；负控复现的临时改动已备份 + cmp 还原，收工时 `git status --porcelain` 为空、HEAD 仍为 `0a09216`。
- 「回执行为证据」的边界：开工基线读数（lint 1536/602 files、react 162/1676、canvas 54/597）声明为施工方复跑，本审查无法回放开工时刻，但其与收工读数的增量自洽（+72 恰等于六个扩展文件的新增用例数、canvas 56-54=+2 恰为两个新文件）提供了强交叉验证。
- 与 P0-A 相同的限制：playwright 不可用，本审查同样未验证真实浏览器旅程；A1/A3 人工项维持 unconfirmed 是如实标注，不是缺陷。
