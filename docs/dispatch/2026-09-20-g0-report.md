# G0 报告：P0-0 v5 契约与负控编排独立复核

包：G0（v5 最终复核）｜日期：2026-09-20｜RunId：20260920-01｜证据目录：`outputs/file-assets/G0/20260920-01/`

**结论：通过（附 29 处最小勘误），无阻断项。** 勘误后的候选记为 **v5.1**（计划指纹 `9c8b4a6b…e2fd`），可作为 P0-0 放行基线。**本包未实施产品代码、未启动 P0-0、未提交/推送。**

> 三类证据严格分开：**① 设计检查**（读文档/读码）；**② 模型实跑**（本证据目录脚本，编排层）；**③ 产品未跑**（P0-0 未实施——无任何产品测试、IDB、浏览器证据）。本回执中每个结论都带上述标签。

---

## 0. 基线与前置核验

- 仓库：HEAD `a2ce72bf122574815bf1b9134253a7bd0349c038`；分支 `codex/file-assets-foundation-20260920`；开工工作树 **137** 条（48 已跟踪修改 + 89 未跟踪，全量清单见 `git-status-start.txt`）。
- 工具链：node v22.22.2（G0 只跑 node 脚本）。⚠ 环境备注：`2026-09-20-file-assets-foundation-progress.md` 记录本环境 pnpm 实际 **11.19.0** ≠ 仓库声明 **10.33.2**（上次误启动遗留）——G0 未以任何包管理命令作证据；**P0-0 开工前须自行核验工具链**。
- **v5 声明指纹核验（全部命中，见 `freeze-pre.txt`）**：

| 文件 | v5/回执声明 | 实测 | 结论 |
|---|---|---|---|
| p0-0-implementation-plan.md | `faf45cf8…a765` | `faf45cf8…a765` | ✅ 一致（v5 即当前交付版） |
| contract-delta.md | `d0fb4ed7…b360` | 同 | ✅ |
| counterexamples.md | `38a2695e…2b54` | 同 | ✅ |
| baseline.md | `df945562…a766` | 同 | ✅ |
| shared-contracts.md | `98ed8d54…ad0f` | 同 | ✅ |
| acceptance-and-backlog.md | `160535a8…8d86` | 同 | ✅ |
| review-receipt.md | （随第四轮追加而变，无声明值） | `32a7f4b2…3a5` | 记录用 |

- 已读：dispatch、p0-0 计划全文、contract-delta、counterexamples、review-receipt、shared-contracts、acceptance-and-backlog、review/review2/review3 及引用证据（`outputs/file-assets-contract-review-20260920-03/` 的 `check-v4.cjs`/`results.json`）、CONTRIBUTING.md；对照真实源码 `directoryTypes.ts`、`handleStore.ts`、`vitest.config.ts`、`packages/react/package.json`。仓根无 AGENTS.md（`.forge/*` 两份不在本包作用域）。
- 未重做已关闭的 CR2-1…CR2-3；未改动其断言；CR2-4A/CR2-4B 已接受部分与 NC-7 分层原样保留。

---

## 1. 设计检查结论（① 设计检查）

| # | 检查项 | 结论 | 落点 |
|---|---|---|---|
| 1.1 | NC-5 三入口已分开：入口 1 进正式套件（PASS/0）；入口 2 旧快照逻辑反例（常态必红、独立运行）；入口 3 生产中性化（正常副本 0 / 中性化非零，一次性证据） | 通过 | E-19、§5「入口编排」表、CD-32 |
| 1.2 | 不变量已条件化：`activeScopeId≠null` ⇒ 恰一 active 且匹配；`=null` ⇒ 零 active（空库与 detach 后为合法） | 通过 | E-17、§4.1 步骤 2、shared-contracts §1.2.3、★CR2-4A 用例 |
| 1.3 | 计数/标题以行为为准（「NC-1…NC-7，共七条」+ 纪律第 4 问） | 通过 | §5 标题/纪律、§6.1、§8 |
| 1.4 | NC-7 证据分层：可判定的「`mutate` 必须同步返回」契约测试 ↔ 真实事务生命周期另行校准（不得外推） | 通过 | §5 NC-7(a)/(b) |
| 1.5 | 出口完整性 E-1…E-19、无消费者自检表、§6 门禁安排、§7 交接表、§8 停止点 | 通过（抽查） | §1.2/§1.4/§6/§7/§8 |
| 1.6 | 保存/离开链未被本包改动；S2F/S2G 契约未触碰 | 通过 | §3.3「不修改」清单 |

**设计检查发现的剩余缝隙**（等价于 G0 任务 4 点名的四项 + 两处连带）：同实例并发未用 `legalAppend`（§4.2）；§5 helpers 导出/导入与句柄形状不一致；`getRawResult` 把已存 `null` 与缺键混为 `empty`；`pick` 覆盖损坏库的例外与 E-5/CD-05「不自动改写」冲突；连带 §3.2 类型归属歧义、I-23 残留 `(root, opts)` 签名。**已按下述最小勘误收口**（不扩设计）。

---

## 2. G0 勘误（29 处文本替换；旧→新全文见 `errata-diff-*.txt`）

| # | 位置 | 旧 → 新（摘要） | 依据 |
|---|---|---|---|
| E1 | 计划 §4.2「R3：并发两次 RMW」（v5 原文 625–633 行） | `[...base.entries, ...rec([id]).entries]`（旧 active 未转 dormant → 两个 active → 被校验器判 `invalid`，正例在正确实现下必红）→ 旧条目先转 dormant 再追加；并断言两次写 `kind==='ok'` | 任务 4「同实例并发也用 legalAppend」；CR2-4A 同族 |
| E2 | 计划 §5 helpers 块 | `legalAppend`/`emptyRec`/`expectThreeUpdates` 缺 `export`（入口 1/2 的 import 注定失败）；`RegistryReadResult` 类型导入未使用；`handleOf` 缺 `getDirectoryHandle` → 补三处 `export`、去未用导入（保留归属注释）、句柄形状与 §4.2 `dirHandle` 一致 | 任务 4「helper export/导入和句柄形状一致」 |
| E3 | 计划 §5 入口 1 用例 | 无 barrier，与「入口 3 使用与入口 1 **完全相同**调用方（两实例、各自 writeChain、barrier）」及 §6.1 G-2「两实例 + 受控交错」矛盾 → 补 `makeBarrier(2)` 受控启动；import 注释补 `makeBarrier` | 同上；保持「只变实现」的单一变量对照 |
| E4 | 计划 §5（入口 3 段落前） | 未写入口 2/3 的夹具安装 → node 环境将以内 `indexedDB is not defined` 类**环境错误**收场（环境错误不算负控命中）→ 新增 ★G0 注记（安装/复位抽到可导入位置） | 任务 2「编排」可运行性 |
| E5 | 计划 §4.2 步骤 1、步骤 2、R2 用例 | `undefined`/`null` → `empty`；`req.result == null` → `empty`（已存 `null` 被折叠为「键不存在」）→ **仅 `req.result === undefined` → `empty`**；已存 `null`（及一切非法值）交由 `classify` → `corrupt`；R2 用例补 stored-null → `corrupt` 断言 | 任务 4「null 与真正缺键不混为 empty」 |
| E6 | 计划 E-5 / §4.4（签名、行为表 ×4、步骤 3/7、新增用例、计数 ×2）＋ shared-contracts §1.2.6 ＋ acceptance G15 ＋ contract-delta CD-05 | `pick()` 允许覆盖损坏记录（`allowOverwriteCorrupt: true`）；设计文档同写「`pick` 可覆盖」→ 删除 opts；`corrupt` 对**所有入口**一致「不使用、不覆盖、不写入」；新增 pick-损坏一致性用例（★G0）；计数 17→18；CD-05 补 G0 注记 | 任务 4/5「corrupt 的 picker 行为与不得覆盖损坏库出口一致」「不隐式覆盖的保守默认」 |
| E7 | 计划 §3.2 清单 | 行文看似 `WorkspaceRegistryEntry/Record/RegistryReadResult` 从 handleStore「新增」，与 §4.2 ⚠（三者定义在 workspaceScope）冲突 → 括注澄清「本文件只 import type，不得重复 export」 | 任务 4「export/导入一致」（归属） |
| E8 | 计划头部 | —（记录用）→ 新增 **v5.1** 版本注记（含 v5 旧指纹、回执与证据目录路径） | 可审计 |
| E9 | shared-contracts §1.2.6 I-23、§4.2 入口一致性段；contract-delta I-23 条目 | `resolveScopeForIdentity(root, opts)` 残留 → 统一为 `resolveScopeForIdentity(root)`；CD-05 注记补充签名同步说明 | E6 连带收口 |

**勘误后指纹（v5.1 锁定基线）**：

| 文件 | 勘误前 | 勘误后 |
|---|---|---|
| p0-0-implementation-plan.md | `faf45cf8…a765` | **`9c8b4a6bc3af346d90e25a608b4b1175e900cf33f75c2ddfadf00fbaea82e2fd`** |
| shared-contracts.md | `98ed8d54…ad0f` | **`b2245f3c78ae0112261ece32d4ebd74c3114d4a241c0cda7a3ce3d3d4323a201`** |
| acceptance-and-backlog.md | `160535a8…8d86` | **`aed43a0e223a90cbfbae2f1dc81bb6bdbc406a33cff7965e2a55ec4a03b94845`** |
| contract-delta.md | `d0fb4ed7…b360` | **`6418708c9ca1682737a1dd56cb6bda1631c20fb18ab0725e76feb048c968cc18`** |
| counterexamples.md / baseline.md | — | 未触碰（`38a2695e…` / `df945562…`） |

**勘误机制（可复算）**：`errata-bundle.txt`（25 对声明）+ `apply-errata.cjs`——先对**全部** pair 做 dry-run 校验（每对须恰好命中 1 次，否则零写入）：首跑因 E4 锚点多一空行被拦截（exit 2、未写任何文件），修正锚点后 25/25 应用；EOL 按目标文件自适应（四文件均 CRLF）；**幂等守卫**复跑 exit 2 安全中止（`errata-edit-log.json` 记录每对命中数与文件前后 SHA-256）。另 **批次 2**（4 处人工编辑，`errata-batch2.txt`；其中 1 处首跑遇 Windows 文件锁 EBUSY，重试成功）。差异全文：`errata-diff-plan.txt`（242 行）等 4 份。

---

## 3. 模型实跑（② 编排层证据 — 非产品）

### 3.1 NC-5 三入口与同族场景（`check-nc5.cjs`，10 个子进程真实退出码，父级 `allMatch=true` / exit 0）

| 场景 | 预期 | 实际 | 关键输出 | 判定 |
|---|---|---|---|---|
| entry1-positive（正常实现 + 入口 1 调用方） | 0 | **0** | PASS；记录 a/b/c、唯一 active=c、合法 | ✅ |
| entry2-legacy-negative（正常实现 + 旧快照调用方） | 1 | **1** | AssertionError；记录 a/c（**恰缺 ws:b**）、合法 | ✅ |
| entry3-control（正常副本 + 入口 1 同一调用方） | 0 | **0** | PASS | ✅ |
| entry3-neutralized（中性化副本 + 同一调用方） | 1 | **1** | FAIL；缺 b 或 c 之一、记录仍合法 | ✅ |
| r3-same-instance-legal（v5.1 §4.2 合法追加） | 0 | **0** | PASS；a/b/c 全保留 | ✅ |
| r3-prefix-must-red（**v5 原文**非法追加） | 1 | **1** | 被 `failed('invalid')` 拒绝（ids=[ws:a]）——证明勘误必要性 | ✅ |
| null-vs-missing | 0 | **0** | `{missing:empty, storedNull:corrupt, garbage:corrupt, valid:ok}` | ✅ |
| corrupt-pick | 0 | **0** | `disk-session('registry-corrupt')`、存储一字未改 | ✅ |
| validator-cases | 0 | **0** | empty/detach 合法；双 active、null+active、mismatch、dupId 全拒绝；非法写 `failed('invalid')` | ✅ |
| promise-mutate | 0 | **0** | `failed('invalid')`、旧记录未被破坏 | ✅ |

要点：entry2 的红与 review3 在 v4 上的实测同形（positive `a,b,c` / negative `a,c`，恰缺 `b`，非非法记录、非环境错误）；entry3 为「同一调用方、只变实现」的对照，正常副本先 0、中性化副本非零**分别留证**；父级汇总不吞子进程结果（逐项 `match`）。

### 3.2 静态/编排文本核验（`check-static.cjs`，44 项全 PASS）

- **S1（10 项）**：默认收集 `tests/**/*.test.{ts,tsx}`（与真实 `vitest.config.ts` 逐字核对）与 counterexample include `tests/counterexamples/**/*.counterexample.ts` **互不匹配**；`tests/helpers/registryConcurrency.ts` 两份配置都不收集；正式用例仍被默认收集；`test` 保持 `vitest run`、新增 `test:counterexamples`；父命令不吞结果纪律与 `echo "exit=$?"` 在文本内。
- **S2（10 项）**：五个 helper 导出齐全；入口 1/2 的 import 清单 ⊆ 导出集合；未用类型导入已去除；句柄形状含 `getDirectoryHandle`。
- **S3（8 项）**：`expectThreeUpdates` 六要素（abc/合法/唯一 active/匹配/2 dormant）；三入口引用同一 oracle；无 `it.fails`/反转断言/宽泛 skip 使用（「禁止条款」行已豁免——首轮 2 项失败为**检查脚本自身过严**，修正后全绿，非文档缺陷）；入口 2/3 载体路径与双跑要求、入口 1 barrier、夹具安装注记齐备。
- **S4（3 项）**：`undefined`→`empty` 唯二处；旧 `== null` 写法零残留；stored-null 用例断言到位。
- **S5（6 项）**：计划/设计文档三层（计划 §4.4、shared-contracts §1.2.6、acceptance G15、contract-delta CD-05）口径一致为「所有入口一律不覆盖（含 pick）」，无 `opts` 残留。
- **S6（7 项，pre-errata 确认）**：同实例非法追加、helpers 缺 export、pick 覆盖例外（计划+设计）、acceptance 旧恢复路径、null 混淆写法——在 v5 原文中**确实存在**（勘误必要性 + 可审计）。

### 3.3 边界声明（重要）

以上均为**编排/文本层**证据：**不是**产品模块验证、**不是**自制 IDB 夹具校准、**不是** TypeScript 类型检查、**不是**浏览器验证。入口 2/3 的产品退出码预期仍属设计预期，待 P0-0 实施后按其 §5 三入口分别实跑。

---

## 4. 负控（G0 范围内、已实跑）

1. **entry2（调用方逻辑反例）**：常态必红已实跑（退出 1、恰缺 ws:b、记录合法）——演示「旧调用方写法本身丢更新」，**不得**作为产品缺陷证据。
2. **entry3-neutralized（生产实现中性化）**：正常副本 0 / 中性化副本非零，两跑分别留证——证明「事务内读 + 合并」是保护来源。
3. **r3-prefix-must-red**：v5 原文 §4.2 用例体在正确实现（含校验器）下必红——G0 勘误必要性的直接证据。
4. **静态 S6**：五项 pre-errata 缺陷确认（非法追加 / 缺 export / pick 覆盖 / opts 残留 / null 混淆）。
5. **未跑**：NC-1…NC-4、NC-6 属 P0-0 门禁范围（产品未实施）——**G0 不宣称其已跑**；G0 仅确认其判定方式在 §5 完整且互不重叠。

---

## 5. 门禁

- G0 无全仓门禁要求：**未运行**任何 typecheck / test / depcruise / lint / budget / gate / build（无源码改动，纯文档 + 只读脚本）。
- 本回执引用的全部退出码来自证据目录脚本本身：`apply-errata.cjs`（应用 0；幂等复跑 2）、`check-static.cjs`（44/44、0）、`check-nc5.cjs`（父级 0；10 子进程逐项匹配）。
- 历史门禁数字（如 20260919-03 全绿）未被引用为本包证据（遵守 §6.1 纪律）。

---

## 6. 未运行项与限制（诚实清单）

- 未运行产品测试 / typecheck / gate / build；无浏览器验证、无 fake-indexeddb、无真实 IndexedDB、无跨标签页（**内存事务模型 ≠ IDB**）。
- 未对真实目录做任何改名/移动/删除；未迁移数据；未运行任何 pnpm 命令。
- 入口 2/3 的产品级退出码、夹具 v3 的真实校准、`isSameEntry` 可用性、事务生命周期（跨任务延迟）——均为 review-receipt §7 既有欠账，本包**未新增实测**、未改变其状态。
- 本回执**未覆盖** G0 之后的任何包（P0-0 起均未启动）。

---

## 7. 残余问题（不阻断，交 P0-0 / 后续）

1. **入口 2/3 夹具安装**：已在计划 §5 注记（E4）——实施时把安装/复位抽到可导入位置；否则「红」可能来自环境错误而非断言。
2. **计数口径**：§4.2 的「新增 14」不含 §5 入口 1 的并发回归用例；另 G-2 行同口径。计数**本就不作判据**（CD-34：以行为为准），如需精确列表以行为集合为准。
3. **工具链**：pnpm 11.19.0 ≠ 声明 10.33.2（上次误启动遗留，未恢复）——P0-0 首步自核。
4. **P1-A 欠账 / `unassociated` 代价**：未变（review-receipt §7 限制 10/11）。
5. **损坏库的「显式重建」**：设计已声明为显式修复入口、不在本包——P0-0 只保守降级，不得顺手实现。

---

## 8. 下一包接口（P0-0 可消费）

- **依据物（v5.1 锁定版）**：`p0-0-implementation-plan.md`（`9c8b4a6b…e2fd`）+ `shared-contracts.md`（`b2245f3c…`）+ `acceptance-and-backlog.md`（`aed43a0e…`）+ `contract-delta.md`（`6418708c…`）；勘误全文、pre/post 快照与全部证据在 `outputs/file-assets/G0/20260920-01/`（索引见其 `README.txt`）。
- **P0-0 实施必做（从 E-19/§5/§8 提炼）**：
  1. 三入口**分别运行、分别留证**：入口 1 进正式套件（PASS/0）；入口 2/3 走 `test:counterexamples` 并记录**真实非零**退出码；入口 3 正常副本先跑 0。默认 `vitest run` 绝不收集反例。
  2. 反例文件先安装同一 v3 夹具（E4 注记），保证「红」来自断言而非环境。
  3. 共用 oracle `expectThreeUpdates` 原样；禁止 `it.fails`、反转断言、`catch` 后 PASS、宽泛 skip、父级 exit 0 冒充。
  4. 两次写**先断言 `kind==='ok'`**（不得因非法记录被 `invalid` 而假通过）；入口 1 用受控启动 barrier（E1/E3）。
  5. `corrupt×pick` 一致性用例（★G0）与 stored-null→`corrupt` 断言随实现落地（E5/E6）。
  6. 不得把模型/编排证据写成产品通过；NC-1…NC-7 逐条按各自「判定方式」跑红；`mutate` 同步返回契约与真实事务生命周期分开标注。
- **禁止**：范围扩张（`rebindDestination`/`*Safe`/上传/索引/人工关联 UI 归 P0-A/P0-B/P0-D/P1-A）；改动设计方向（注册表新键、`isSameEntry` 唯一证据、8 条上限、三入口一致、损坏库不覆盖、`'workspace-root'` 裸句柄）。

---

## 9. 交回声明

- 未暂存、提交、推送、部署、发布；未执行 pull/rebase/reset/stash。
- 未改产品源码（`packages/react/src`、`apps/**`）与正式测试；未启动 P0-0。
- 本包产物：**① 本报（唯一回执）**；**② 证据目录 `outputs/file-assets/G0/20260920-01/`**；**③ 勘误 29 处**（4 份文档：p0-0 计划、shared-contracts、acceptance-and-backlog、contract-delta；旧→新与哈希记录完备）。
- 工作树 137 条 → 本报落地后 138（新增唯一 1 条即本报；证据目录并入既有未跟踪路径计数）。
- **到此停止**，等待主控评审；通过后由用户另发 P0-0，不由本包实施。
