# P0-0 独立复核报告（只读）

- 复核对象：`file-assets-foundation` / **P0-0「工作区身份与存储基础」冻结候选**
- 复核日期：2026-09-20
- 复核方式：**只读独立复核**（未修改候选、未修复缺陷、未删除任何证据）
- 主仓库（WORKTREE_PATH）：`<workspace>`
- 候选（CANDIDATE_REF）：`codex/p0-0-frozen-20260920` = 提交 `3cdcdc3d39b243adc0a2485d0ccdab302613ff60`（父 `a2ce72b…`，tree `4fdf3ee7…`）
- 清单（MANIFEST_PATH）：`outputs/file-assets/P0-0/frozen-20260920/manifest.sha256`（+ `manifest.worktree-asfrozen.sha256`）
- 交付报告（P0_REPORT_PATH）：`docs/dispatch/2026-09-20-P0-0-report.md`
- 独立 worktree：`<workspace>-p0-0-review`（`git worktree add <path> codex/p0-0-frozen-20260920`，793 文件检出）
- 本次复核证据：`outputs/file-assets/P0-0/frozen-20260920/review/`（脚本、日志、退出码；未触碰 `20260920-01/`）

---

## 0. 结论

**ACCEPT —— 可作为 S1–S5 的基础候选。**

判定依据：候选内容经**三重哈希交叉核对**无误；白名单**恰为 13 文件、无清单外改动**；在**独立 worktree** 中类型检查 / P0-0 三套测试 / react 全包 / 定向 lint / depcruise **全绿**；负控在候选树上**重建并复现 8/8 非零 + NC-5 三入口 0/1/0/1**。
**所有 P0-0 归属项均通过**；两项非绿项经归因**均非 P0-0 缺陷**（见 §6.4、§7），已作为「前提与遗留」列出，需上层处置。

---

## 1. 前置校验（任一不存在即 STOP）

| 前置 | 结果 |
|---|---|
| ① WORKTREE_PATH | ✅ 存在 |
| ② CANDIDATE_REF | ✅ 可解析（`0x3cdcdc3d…`；对象类型 = commit） |
| ③ MANIFEST_PATH | ✅ 存在（3122 B） |
| ④ P0_REPORT_PATH | ✅ 存在（23764 B） |
| ⑤ 本报告路径 | ✅ 复核前**不存在**（未覆盖既有文件） |

---

## 2. ① 清单 SHA-256 核对（只读）

复算命令：`git cat-file blob <ref>:<path> | sha256sum`（主清单口径：git blob / LF 归一化）。

| 核对项 | 结果 |
|---|---|
| 主清单 `manifest.sha256` 13 条逐条比对 | ✅ **13/13 OK，0 失配** |
| `manifest.worktree-asfrozen.sha256` 13 条对主工作树比对 | ✅ **13/13 OK，0 失配** |
| 清单条目数 | ✅ 13 |
| **独立 worktree 实际字节**核对（`git hash-object --path=<p> <p>` → blob SHA-1） | ✅ **13/13 与清单附录 SHA-1 逐一相同** |

> 说明：主工作树与复核树字节一致（blob 口径），故清单对两棵树同时成立；`core.autocrlf=true` 下检出字节差异不影响 blob 口径复算（P0-0 报告 §6.1 已声明）。

---

## 3. ② 白名单 / 无清单外改动核对

```
git diff --name-status a2ce72b codex/p0-0-frozen-20260920   →  恰 13 条
git diff --name-only  … -- packages/react/src/index.ts      →  空（0 条）
comm -3 <实际 diff 集合> <清单集合>                          →  空（两集合完全一致）
```

| 核对项 | 结果 |
|---|---|
| 候选相对基线的改动条数 | ✅ **恰 13**（6 M + 7 A） |
| 清单集合 == 实际 diff 集合 | ✅ 完全一致（无清单外、无遗漏） |
| `packages/react/src/index.ts` 是否被夹带 | ✅ **未夹带**（0 条） |
| 候选树文件总数 | ✅ 793 = 基线 786 + 7 新增 |
| 工作树 156 项脏改动中非 P0-0 部分 | ✅ 未被纳入候选（工作树基线 156 未因复核变动） |

---

## 4. ③ 独立 worktree 内的运行结果

**环境准备**（复核侧行为，不改候选内容）：`node_modules` 以 junction 复用主仓库（root + 4 包）；自引用导入所需 `packages/react/dist` 由 `tsc -p tsconfig.build.json` 构建（与 CI 一致）。

| # | 项目 | 命令（在独立 worktree 内） | 结果 | 退出码 |
|---|---|---|---|---|
| T1 | 类型检查 | `packages/react: node node_modules/typescript/bin/tsc -b tsconfig.json`（含引用 kernel / free-canvas） | 0 错 | **0** |
| T2a | workspace-scope | `node node_modules/vitest/vitest.mjs run workspace-scope` | **9 passed (9)** | **0** |
| T2b | handle-store | `… run handle-store` | **29 passed (29)** | **0** |
| T2c | directory-host | `… run directory-host` | **51 passed (51)** | **0** |
| T3 | react 全包 | `… run --pool=threads` | **151 files / 1506 passed** | **0** |
| T4 | 定向 lint（候选 12 个代码文件） | `node <root>/node_modules/@biomejs/biome/bin/biome lint <12 文件>` | `Checked 12 files … No fixes applied.`（0 告警） | **0** |
| T5 | budget | `node scripts/check-code-budget.mjs` | **2 项超预算**（asCast 32/31、bigFiles 5/4） | **1** |
| T6 | depcruise | `node node_modules/dependency-cruiser/bin/dependency-cruise.mjs packages apps --config .dependency-cruiser.js` | `✔ no dependency violations found (514 modules, 1492 dependencies cruised)` | **0** |

**测试数差异说明**：候选树 react 全包 1506 例（主工作树 1545 例）——差值为其它批次**未纳入候选**的新增测试文件（未跟踪），非用例丢失。

---

## 5. ③ 负控（在候选树上重建后复算）

隔离副本由 `negatives.py` **在复核树内、以候选源码为源**重建（`.workbuddy/p0-0-negatives/<NC>/react`），单点中性化；复算脚本镜像到复核树同相对路径，全部产物落复核树内（`20260920-01/` 既有证据零改动）。

### 5.1 NC-1 … NC-7（期望：子命令真实退出码全部非零）

| NC | 中性化点 | 退出码 | 日志内 AssertionError | 日志内环境错误 |
|---|---|---|---|---|
| NC-1 / handle-store | 注册表写回旧键 | **1** | 9 | 0 |
| NC-2 / directory-host | 按目录名判同一性 | **1** | 2 | 0 |
| NC-3 / handle-store | 损坏记录按 empty | **1** | 2 | 0 |
| NC-3 / directory-host | 同上 | **1** | 2 | 0 |
| NC-4 / handle-store | 读/打开失败折叠 empty | **1** | 2 | 0 |
| NC-4 / directory-host | 同上 | **1** | 2 | 0 |
| NC-6 / directory-host | 删 register 分支 | **1** | 1 | 0 |
| NC-7 / handle-store | 接受异步 mutate | **1** | 1 | 0 |

→ **8/8 非零，且红因全部为预期断言失败（环境错误计数 0）** ✅

### 5.2 NC-5 三入口

| 入口 | 期望 | 实测 | 红因 |
|---|---|---|---|
| 入口 1（默认套件内并发回归） | 0 | **0** | —（29/29 绿） |
| 入口 2（旧快照反例，独立 config） | 非零 | **1** | `AssertionError: expected [ws:a,ws:c] to deeply equal [ws:a,ws:b,ws:c]` |
| 入口 3-control（正常副本） | 0 | **0** | — |
| 入口 3-neutralized（读取移出事务） | 非零 | **1** | 同类 `AssertionError` |

→ **0 / 1 / 0 / 1，与交付声明完全一致，且在候选树上独立重建后复现** ✅

---

## 6. 复核中的重要观察

### 6.1 复核环境导致的一次假红（已排除）
首次运行 react 全包时 `tests/demo-plugin.test.ts` 报 `Failed to resolve import "@mindcanvas/react"`。根因：该包 `package.json` 为**自引用包名**（`name=@mindcanvas/react`、`main/exports → ./dist/*`），而复核树尚未构建 `dist`。补建 `dist`（`tsc -p tsconfig.build.json`）后：单文件 → **2 passed / 退出 0**；全包 → **1506 passed / 退出 0**。**属复核环境准备问题，非候选缺陷。**

### 6.2 budget 红项归因（关键，结论性）
在**三个纯净导出**（`git archive`，无构建产物、无复核附加物）上分别复算，结果逐项一致：

| 树 | asCast | bigFiles | >600 行文件 |
|---|---|---|---|
| **基线 `a2ce72b`** | 32 / 31 ↑ | 5 / 4 ↑ | MindmapStage 2435、**frameLayout.ts 939**、index.ts 648、edgeRouting 1235、MapView 2636 |
| **候选 `3cdcdc3d`（基线+P0-0）** | **32 / 31 ↑** | **5 / 4 ↑** | **与基线完全相同** |
| 主工作树（基线+P0-0+其它批次） | 31 / 31 持平 | 4 / 4 持平 | 4 件（不含 frameLayout.ts） |

→ **候选与基线逐项完全相同 ⇒ P0-0 贡献 0 债务增量**；红项源自基线（`packages/kernel/src/layout/frameLayout.ts` 基线上 938 行，主工作树经其它批次重构为 436 行）。**非 P0-0 归属。**

### 6.3 报告口径需更正
`2026-09-20-P0-0-report.md` §6.2 / §7 的「budget 全部在预算内」基于**主工作树**；对**冻结候选树**（隔离）**不成立**（红项同上）。建议回执加注「预算结论为主工作树口径；隔离候选树沿用基线红项」。

### 6.4 环境异常（引用易失，复核期间再次发生）
复核期间 `refs/heads/codex/**` **再次被环境回滚**（`git status` 由 156 膨胀至 888 = HEAD 悬空），导致独立 worktree 的 HEAD 一度无法解析。已按 P0-0 报告 §2.3 的方法**直接写引用文件**恢复两个引用（`3cdcdc3d…` / `a2ce72b…`），恢复后主工作树 `status` 回到 **156**。
→ **判定：环境级**，非候选缺陷；**唯一可靠锚点 = 提交 SHA `3cdcdc3d…`**（对象持久）。复核未因此丢失任何结论。

---

## 7. 前提与遗留（**非阻断项**，需上层处置）

| # | 事项 | 性质 | 处置建议 |
|---|---|---|---|
| C1 | `budget` 在隔离候选树为红（asCast 32/31、bigFiles 5/4） | **继承基线、P0-0 零增量**（§6.2 已证） | 合入/发布前由上层裁定：先并入其它批次的债务削减，或调整基线口径；**不应记在 P0-0 账上** |
| C2 | `packages/react/src/index.ts` 公共导出接线**未纳入**候选 | 交付报告 §4 的有意排除，待**主控裁定** | 按报告 §4.3 三路径之一裁定；接线落地后公共面方可消费 |
| C3 | 交付报告 §7「预算全绿」口径 | 文档准确性 | 按 §6.3 更正为「主工作树口径」 |
| C4 | 该沙箱 `refs/heads/**` 写入易失 | 环境 | 一切复核/后续工作**以提交 SHA 为锚**；引用缺失时用直接写引用文件恢复 |

**对 S1–S5 的直接影响**：S1–S5 若在本候选分支上开发，**不得以 `gate` 全绿为前提**（budget 沿用基线红项）；消费 P0-0 接口需经**深路径** `packages/react/src/edit/{workspaceScope,handleStore,directoryHost,directoryTypes}.ts`，直至 C2 接线落地。

---

## 8. 未覆盖 / 未运行项（如实声明）

1. **真实浏览器 CDP 验证未重跑**（原证据为用户侧 Chrome headless 10/10；本复核未复现浏览器链路）。
2. **kernel / free-canvas / canvas 三包套件未在本候选树重跑**（仅跑了 react，P0-0 归属范围；canvas 套件不在本包范围）。
3. **`pre-commit` hook 未在候选提交上触发**（提交经 git plumbing 构造；已用同树 depcruise + lint 等效覆盖）。
4. **完整 `pnpm gate` 单命令端到端未取得干净退出**（环境级 vitest 收尾挂起；以分项运行等效覆盖）。
5. **未推送远端、未开 PR**。

---

## 9. 只读合规声明

- **未修改候选**：候选提交 `3cdcdc3d…`、其 tree、其 13 个文件字节均未变动（§2 三重核对）。
- **未修改产品/测试源码**：主工作树 `git status` 基线 156 → 复核结束仍为 **156**（唯一新增为本报告……见 §10 附录）。
- **未删除证据**：`outputs/file-assets/P0-0/20260920-01/` 30 个条目全程未改动（只读读取）。
- **复核侧新增（均在候选之外，可安全清理）**：
  - 独立 worktree `<workspace>-p0-0-review`（`git worktree` 注册）
  - 该树内 `node_modules` junction ×5、`dist/`（react，构建产物）、`.workbuddy/p0-0-negatives/`（负控副本）、`outputs/file-assets/P0-0/20260920-01/`（镜像的负控脚本）
  - 主仓库内复核证据 `outputs/file-assets/P0-0/frozen-20260920/review/`
  - 主仓库内纯净导出 `.workbuddy/p0-0-pristine`（候选）、`.workbuddy/p0-0-pristine-base`（基线）（均已被 git/biome/budget 忽略）
  - 本报告

---

## 10. 附录：复核命令与证据索引

**关键命令**（可在 `review/run-review-1.sh`、`run-review-2.sh`、`run-review-3.sh` 复跑）

```
# 清单与白名单
git cat-file blob codex/p0-0-frozen-20260920:<path> | sha256sum
git diff --name-status a2ce72bf122574815bf1b9134253a7bd0349c038 codex/p0-0-frozen-20260920
# 独立 worktree
git worktree add <workspace>-p0-0-review codex/p0-0-frozen-20260920
# 运行（复核树内）
cd packages/react && node node_modules/typescript/bin/tsc -b tsconfig.json
cd packages/react && node node_modules/vitest/vitest.mjs run workspace-scope   # 同 handle-store / directory-host
cd packages/react && node node_modules/vitest/vitest.mjs run --pool=threads
cd packages/react && node node_modules/typescript/bin/tsc -p tsconfig.build.json  # 自引用需要 dist
python <mirror>/negatives.py && bash <mirror>/rerun-nc-negatives.sh && bash <mirror>/rerun-nc5-entries.sh
node scripts/check-code-budget.mjs
node node_modules/dependency-cruiser/bin/dependency-cruise.mjs packages apps --config .dependency-cruiser.js
```

**证据索引**（`outputs/file-assets/P0-0/frozen-20260920/review/`）

- `run-review-1.sh` / `run-review-2.sh` / `run-review-3.sh`：复核运行器（可复算）
- `run1-codes.txt` / `run2-codes.txt` / `run3-codes.txt`：各步真实退出码
- `nc-exit-codes.txt` / `entry-exit-codes.txt`：负控与三入口退出码
- `logs/`（T1–T6）、`logs2/`（N0–N2）、`logs3/`（S1–S5：构建 / 单文件复跑 / 全包复跑 / 纯净 budget / **基线 budget**）
- 独立 worktree 内：`outputs/file-assets/P0-0/20260920-01/{nc,nc-exit-codes.txt,entry-exit-codes.txt,logs}`

---

## 11. 结论复述

**ACCEPT。** 候选 `codex/p0-0-frozen-20260920 @ 3cdcdc3d…` 内容与清单完全一致、无清单外改动、P0-0 归属的全部测试/类型检查/lint/depcruise/负控均通过，**可作为 S1–S5 的基础候选**。
前提：§7 的 C1（基线继承的预算红项）与 C2（`index.ts` 接线待裁定）须由上层处置；C4（沙箱引用易失）须以提交 SHA 为锚。
