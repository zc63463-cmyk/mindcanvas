# P1-A 施工回执 —— 文件导航与归档（+ 容器一致化/人工关联/弱匹配移除三 rider）

- 日期：2026-09-24
- 派单书：[2026-09-24-P1-A-dispatch.md](2026-09-24-P1-A-dispatch.md)
- 性质：深度外派施工（六切片 + 三 rider + 判别性负控 + 真浏览器人工旅程）
- 提交链：
  - `ed09c48` feat(p1a)：文件导航六项 + 三 rider（27 文件，+2575/-352）
  - `c10bd88` test(p1a)：四新测试文件 + 标题/弱匹配接线用例 + 两真浏览器脚本（8 文件，+2291）
  - 本提交 docs(p1a)：本回执 + 负控/真浏览器证据
- **开工锚定**（派单书要求，开工时 `git status --porcelain` 为空）：
  HEAD `7d584ca86d60f3a6b00c8ac3b3d3f921895209de4`；
  `HEAD:apps` = `d6c61d703c7303460604f754b2bd02c730f76491`；
  `HEAD:packages` = `8d99bf47b189c97365f3a6e325b16f11896f0c80db64`
- **收工锚定**（本 docs 提交前，产品树锚）：
  HEAD `c10bd8893317e967f7c42ed800c6b07b7716a479`；
  `HEAD:apps` = `b02490d2509cfa35fc5e9754b2bd02c730f76491`；
  `HEAD:packages` = `c8aa85b9e7498fa1351405b16f11896f0c80db64`
- 开工说明：接手时工作树含上一会话的**未提交 P1-A 半成品**（六切片 + rider 代码与 4 个新测试文件已成形，但①负控/真浏览器/回执未做；②存在真实缺陷：`onMoveFailed` 声明了但 FileManager 未接线，归档失败抛 unhandled rejection；③门禁双超：bigFiles 5/4、console 5/4）。本回执覆盖的是「接管→补正→验证→收口」全量结果，半成品来源无法用提交证明，如实标注。

---

## 0. 结论

backlog §514 六项范围 **全部交付**；rider-A/B/C **全部交付**。
自动化：canvas 62 文件 / **740** 用例、react 162 / 1778、kernel 75 / 708 全绿；
判别性负控 7 条逐条做了「变异落盘计数 → 转红（exit 1）→ 备份 cmp+sha256 还原（禁 git checkout）→ 回绿」六要素；
真浏览器（vite build 产物 main-DQeEtSpE.js）：a11y 键盘走查 + X1 归档旅程脚本 28/28、容器几何脚本 17/17。
门禁八项预算持平/更优，lint 1529 ≤ 基线 1530，depcruise 0 violations，build exit 0。

---

## 1. §1.1 已前置交付项核对登记（不重复建设）

| 派单书盘点项 | 核对结果（代码实读） |
|---|---|
| 「最近」唯一入口按 openedAt 降序、null 末尾、不回落 mtime | P0-D 已交付，**未重建**。本轮核对到真实覆盖：`doc-index.test.ts` compareRecent null 排序/formatRecentWhen/0 非哨兵 4 例 + file-manager-doc-index 「未记录打开时间」1 例；本轮另以**变异方式**重验其判别力（见 §3 NC-3） |
| 收藏按 docKey/lineageId，scope 隔离 | P0-D 已交付，零改动 |
| 「创建副本」入口 | P0-D 已在右键菜单（`data-menu-duplicate` → currentDocOps.duplicate），本轮只核对、未重建 |
| 最近 openedAt 接线（FileManager:221） | 已交付，openNode 仍经 markOpened 推进；**未碰** |
| 收藏迁移/`useStarredKeys` | 已交付，FileManagerHistories/Tree 沿用 |
| 创建副本入口 P0-D 前置 | 同上 |

新增的每个公共符号都有本包内真实消费者（收口判据 §6-6 自查）：接管时发现并删除了两处无消费者死代码——`ARCHIVE_COPY.done`（成功无 toast 是刻意设计，与改名/拖拽同惯例，文案删除）与 `treeContainerKey`（容器从不获得焦点：roving 模式下行恒有 tabindex=0，且键位挂在行上）。

---

## 2. 六项 + 三 rider 逐项证据

### ① 当前文档高亮与状态行
- 判据 = **完整路径**（`FileManagerTree.tsx`：`ctx.currentPath === n.fullPath`），非名字/非 docKey；行上 `aria-current="true"` + `data-doc-current` + 「◀ 当前」徽标 + 高亮背景。
- 状态行 `BreadcrumbBar`（FileManagerTreeTools.tsx）：`data-doc-status` 带 `role="status"` / `aria-live="polite"`。
- 证据：file-manager-keyboard.test.tsx「aria-current=true 与徽标」「★负控 2 同名不同目录唯一高亮」；真浏览器 A6/A7（同名文档对只亮一份）。

### ② 搜索内部标题（限已索引文档，无新扫描）
- 消费侧：`useFileTreeView.titlesByPath` 从 `index.listDocs()` 的既有 `title` 贴到节点；`filterTree` 加 `n.title` 一路命中（祖先链保留规则不变）。搜索框 placeholder：「搜索文件名、路径或标题…（标题仅搜索已记录的文档）」。
- **生产链路补正（接管时发现的真实缺口）**：P0-D 只交付了 `title` 字段，生产代码没有任何写入点（openDoc/saveDoc 均不传 title），功能在生产上无数据。本轮按 §3.4「不加文件读取、不加扫描」补最小切片：新增纯函数 `docTitle.ts/firstHeadingOf`（与 kernel `RE_HEADING` level-1 同形），在 MindmapStage **已加载快照**上取首个 H1，经 `DocIndex.registerDoc` 新增的可选 `title` 字段登记（undefined=保持原值；显式 null=以快照为准）；真实保存路径 saveDoc 同步传 title。打开即有标题、不推进任何时间戳（单测钉死 openedAt/savedAt 不被重推）。
- 证据：doc-index.test.ts +7 例（纯函数 8 边界 + registerDoc 三态）、file-manager-keyboard.test.tsx 标题命中/placeholder 2 例；真浏览器 B3 搜「北极星」命中「当前文档.mm.md」。

### ③ 排序依据可选
- `sortTree(mode: 'name'|'mtime')`，默认 name；工具条 `data-fm-sort-select` + `data-fm-sort-active` 列头写明当前依据。
- 「最近」视图不渲染工具条（UD-2 不动 openedAt）；「收藏」同。
- 证据：keyboard 测试 3 例（默认名称/mtime 真重排/最近无工具条）；真浏览器 B1/B2。

### ④ roving tabindex + tree 语义
- 纯函数状态机 `fileTreeKeyboard.ts`：`intentFor`（↑↓/→展开或进首子/←折叠或回父/Home/End/Enter/F2/Delete/Esc）、`visibleRows`、`reconcileFocus`（原 key 消失落邻近行；空树 null）。
- 组件：容器 `role="tree"`；目录行/文档行 `role="treeitem"` + `aria-level`/`aria-expanded`/`aria-selected`；全树恒恰一行 tabindex=0（reconcile 保证无死入口）；行内 onKeyDown 消费意图，**没有 window 级树键监听**（R1-3 教训）。
- 重命名输入框 `aria-label` + 在输入框自己的 onKeyDown 里对方向键/Home/End/Delete/F2 `stopPropagation` 让位；Enter blur、Esc 取消。
- Esc 关面板：文件树为空时由状态机给 close-panel 意图；非空时由 FileManagerModal 既有 window Esc 关闭（两处不重复）。
- 证据：纯函数 10 例 + 交互 8 例（含真机负控 1）；真浏览器 A1–A10 全键位。

### ⑤ 显示其他文件
- 宿主：`scan(force, { includeOtherFiles })` 同一遍遍历回传 `unopenable`（`directoryTypes.ts` + `directoryHost.ts`），缓存键含该口径；不支持文件**不计入** st.count（不挤 2000 导图预算）。
- 前端：开关默认关；开后重扫 → `withOtherFiles` 把不支持项摘出主树挂成根层虚拟分组（key `other:\u0000`，与真实路径不撞），默认折叠；隐藏文件归入该组。
- 行：`data-doc-unopenable`、opacity 0.45 + not-allowed、星标禁用、不可拖拽（dragStart 直接 return）、onDragOver/onContextMenu 全禁；点击给 `role="status"` 提示「<名>：非导图文件，本应用不打开。」且绝不调 onOpenFile；Enter 键同路径。
- 证据：keyboard 测试开关/默认折叠/只出现一次/负控 6；真浏览器 B4 三条。

### ⑥ 改为归档
- 纯逻辑 `fileArchive.ts`：`ARCHIVE_DIR='_归档'`；`archiveTargetDir` 根层→`_归档`、嵌套→`_归档/<原目录层级>`（防不同子目录同名互撞）；`isArchivedPath` 任一段匹配（含嵌套归档）；`archiveRefusalOf` 三拒绝（未挂载/目录/已归档），零 I/O。
- `_归档/` 按**普通目录**呈现（无隐藏、无特殊标记）；扫描器对 `_` 前缀无特殊过滤（§3.1 自检点已核实，SCAN_SKIP_DIRS 只有 node_modules/.git/.obsidian/.vscode/dist/build/.cache）。
- 两个入口同一编排：删除确认条旁 `data-fm-confirm-archive` 与右键 `data-menu-archive` 都调 `archive.request`；已归档文档不给入口（零 I/O 空操作不给按钮）。
- **单一路由点**：归档落点只调 `dropIntoRouted`（当前文档→currentDocOps.move 租约+目的地重绑+冲突三选；非当前→既有 moveFile）。接管时代码在 useFileArchive 里重复了一遍同一分流（两处同一判定、注释自警「迟早分叉」），已删除冗余、判定收归路由一处（NC4 证明归档没有自写第二条冲突策略）。
- 目录创建：非当前路径由宿主 `createFile → uniqueName/dirAt` 链路建 `_归档/`；当前文档路径由 `moveFileSafe` 内部 `dirAt(root, path, {create:true})` 建。
- **失败出口补正（接管时发现的真实缺陷）**：`useFileTreeOps.dropInto` 的 try/catch 与 `onMoveFailed` 是半成品——FileManager 调用点**漏传** `onMoveFailed`，归档权限失败时 `workspace.moveFile` 抛错变成 **unhandled rejection**（测试 13 例表面全绿但 Vitest 记 1 error；scratch 探针实测该形态退出码=1，确有判别价值）。已接线：`classifyFileOpError(e) → failNoticeOf()`（FILE_OP_FAIL_COPY 唯一文案源）→ treeNotice；失败分支不 reload，树不出现幽灵条目。
- 证据：file-manager-archive.test.tsx 13 例（落点纯函数/两入口同落点/分流/首次建目录/非删除/冲突三选不绕过/失败无幽灵/普通目录）；真浏览器 C1–C5（磁盘文件表断言源消失+`_归档/子目录/嵌套.mm.md` 出现）。

### rider-A 容器一致化（P0-C-review §7-a 阻塞项，本包收口）
- 唯一事实源 `floatingNotices.ts`：`FLOATING_SLOTS` 四槽（top 64/100/16/10、zIndex 5/5/6/3、水平居中）+ `NoticeDismiss`（auto(ms)/manual）+ `floatingBaseStyle`（padding 8px14/radius8/fontSize12 统一）+ `floatingToneStyle`。
- `FloatingNotice.tsx`：组件内零位置字面量；定时器在组件内、`children` 入 deps（内容变即重置，与旧 `[commandNotice]` 同语义，biome-ignore 已注明判别由单测承担）。
- MindmapStage 四条浮条全部改挂 FloatingNotice（守卫断言调用数恰为 4）；旧的两处手写 setTimeout 已移除。
- **消退语义逐值保留**（未按 tone 一刀切）：命令告警 4s 自动、预方向 2.5s 自动、资产条点击关闭（manual）、规模条条件存续期间常驻（manual）。接管时模块头注释与表自相矛盾（注释称「错误类一律常驻」但表里命令条是 auto），已改注释贴合表（表与测试才是正确期望，未改产品行为）。
- 内联条：`FileManagerViews.tsx` ProjectionFailure/MigrateFailed 两处与 `FileManagerInlineBars.tsx` TreeErrorBar 全部归 `inlineBarStyle`（margin 0 12px 8px / padding 6px 8px / radius 6 / fontSize 11）。
- 证据：floating-notices-consistency.test.tsx 8 例（槽位几何逐值/渲染几何一致/消退 auto vs manual/源码守卫 3 条）；真浏览器 verify-fm-containers 17 例 DOM 几何断言。

### rider-B 人工关联面板（shared-contracts §1.2.8）
- `workspaceAssociate.ts`（纯逻辑）：readoutOf 四态分开；associationRefusalOf（record-not-ok/not-found/already-confirmed）；`applyUserConfirmed` 只追加 `via:'user-confirmed'` 证据行（scopeId 不变、不新建身份、句柄原样、其余转 dormant 保 active 唯一）。
- `AssociateWorkspacePanel.tsx`：入口挂 StorageBar 作用域区（`data-assoc-open`）；四态独立文案（corrupt「不会改写」/unavailable 不并入空）；写回走 `writeWorkspaceRegistry(mutate, {kind:'unchanged'})` 单事务 RMW，读数非 ok 时 mutate 零写入；成功后面板收起；用户语言不暴露 scopeId（仅 data 属性）。
- **scopeIdentity 判定链零改动**：面板不参与 reuse/register/degrade/refuse（无任何判定链 import）。
- 证据：workspace-associate.test.tsx 12 例。

### rider-C 移除历史池 name 弱匹配（P0-D 报告 :400 债务 #6）
- `FileManagerViews.HistoryPool` 的 `exact` 字段与「名字相等自动绑定」分支已删（候选 `docKey/name` 两字段）；FileManager 传入的 candidates.name = fullPath，排序只作提示；默认占位、用户显式选择才写 `relink`（grantRelinkEvidence user-confirmed + legacyKeys 追加）。
- **fullPath 精确快捷路径保留为排序提示，永不自动绑定**（§3.8）。
- 证据：file-manager-doc-index.test.tsx +3 例（默认占位/点选证据写对且不串另一条/即刻生效+legacyKeys 含旧键）。

---

## 3. 负控六要素归档索引

目录：`docs/dispatch/evidence/2026-09-24-p1a-negative-controls/`（每条 RED + GREEN 两份日志）。
统一规程：变异只动**生产守卫**、正确断言一字不改；变异后先 grep 落盘标记计数；转红判据为命令真实退出码非零（RED 日志含 `exit=1` 的执行记录与失败用例名）；还原一律 `/tmp` 备份 + `cp` 覆盖 + `cmp` 字节校验（**禁 git checkout**，CRLF 工作树纪律）+ sha256 复算；还原后回绿与全量一致。常态必红反例**未进**默认门禁（全部为生产实现中性化，随默认套件运行）。

| # | 守卫（中性化方式） | 落盘标记 | RED 退出码 / 转红用例 | 还原校验 |
|---|---|---|---|---|
| 1 | FileManagerTree 重命名输入框的 5 键 stopPropagation 整块注释 | `ev.stopPropagation();` 计数=2（原 3） | 1 /「★负控 1：焦点在重命名输入框内，↑/↓ 不移动树焦点、Delete 不弹确认条」 | cmp OK，sha256 `0dd8bb96…254e`，回绿 27/27 |
| 2 | 高亮判据 `=== n.fullPath` → `endsWith(n.name)`（退回名字后缀） | `endsWith(n.name)` 计数=1 | 1 /「★负控 2：两个同名不同目录的文件，只有真正打开的那一份高亮」 | cmp OK，回绿 27/27 |
| 3 | compareRecent 的 `a.openedAt===null → 1` 改为 `-1`（null 提前） | 变异行计数=1 | 1 /「compareRecent 的 null 分支按 savedAt 降序稳定排列」（P0-D 既有覆盖，按派单书登记并以变异重验判别力） | cmp OK，回绿 75/75 |
| 4 | dropIntoRouted 当前文档分流条件加 `false &&`（归档落回静默移动路径） | 变异计数=1 | 1 /「★当前文档归档→走 currentDocOps.move」+「★负控 4：落点已有同名文件不静默覆盖」2 例 | cmp OK，回绿 13/13 |
| 5 | useFileTreeOps.dropInto 的 try/catch 失败分支整块删除 | `try {` 计数=0 | 1 / **Unhandled Rejection**（13 用例本身仍 passed，但 Vitest Errors=1、退出码 1）：`E-PERMISSION: 权限被拒` | cmp OK，回绿 13/13、0 error |
| 6 | FileManager `onUnopenable` 改为 `() => undefined`（点击静默） | 变异计数=1 | 1 /「★负控 6：点击不支持文件 → 提示而非静默、也不打开」 | cmp OK，回绿 27/27 |
| 7 | MindmapStage command-notice 浮条退回手写 `<div style="top:64…">` | `top: 64,` 计数=1 | 1 / 源码守卫 2 例（硬编码位置 + 浮条数不等于 FloatingNotice 调用数） | cmp OK，回绿 8/8 |

**关于负控 4 的归因说明**：派单书示例为「中性化冲突判定分支」。归档唯一的冲突处理在当前文档编排层（moveFileSafe uniqueName/三选），归档自身无冲突判定可中性化——中性化**分流守卫**（强制当前文档也落静默移动路径）后，「归档不得绕过三选编排」的正确期望直接转红，故中性化点选在分流，判据仍是同一份正确期望。

---

## 4. 真浏览器证据（vite build 产物，非 dev server）

- 产物：`apps/canvas/dist/assets/main-DQeEtSpE.js`（532.77 kB / gzip 172.05 kB）；snapshotCheck 三查通过（页面实际加载 == dist 引用 == 最新构建）。
- 服务：`vite preview --port 5175`；替身仅顶替 showDirectoryPicker 与磁盘 I/O（tools/lib/browserHarness.mjs 内存盘，同步语义）。

### 4.1 a11y 键盘走查 + X1 归档旅程 —— 28/28 PASS（3 项 unconfirmed）
`docs/dispatch/evidence/2026-09-24-p1a-e2e/p1a-journey-PASS.log`（`tools/verify-p1a-journey.mjs`，exit 0）
- A1–A10：role=tree/treeitem/aria-level=1、Tab 唯一焦点行、↓ 真迁移（activeElement）、→/← aria-expanded、Home/End、aria-current 同名唯一、F2 输入框+**真机负控 1**（输入框内 ↓ 树焦点不动/无删除条/未重进改名）、Esc 退出改名、Enter 打开（文档栏切换）、Esc 关面板。
- B1–B4：排序列头依据切换、「最近」无排序条、标题搜索命中、其他文件分组/灰显/点击提示且不打开。
- C1–C5：右键「改为归档」→ 磁盘表断言源消失 + `_归档/子目录/嵌套.mm.md` 出现（目录真实创建）+ 树中普通目录可见 → 从归档再打开（文档栏=嵌套.mm.md）；删除确认条旁入口同一编排落 `_归档/待归档.mm.md`；已归档文档无归档入口。
- unconfirmed（如实）：真实 OS IME 组合键、触控/拖放归位、真实 FSA 权限被拒（失败路径由单测负控 5 覆盖）。

### 4.2 rider-A 容器几何断言 —— 17/17 PASS（1 项 unconfirmed）
`docs/dispatch/evidence/2026-09-24-p1a-e2e/fm-containers-PASS.log`（`tools/verify-fm-containers.mjs`，exit 0）
- 四浮条真实 getBoundingClientRect/computed z-index：asset-notice top=100/z5/居中且 1.5s 常驻、点击关闭；pre-dir-hint top=16/z6/2.8s 自动消退；command-notice top=64/z5/4.5s 自动消退；scale-notice top=10/z3/3s 常驻（夹具 88,573 节点落 >50K 档，槽位行为与 >20K 档同一）。
- 内联三条几何同 `inlineBarStyle`（0px 12px 8px / 6px 8px / 6px）：树侧提示 + 配额下投影/迁移失败条。配额手段为**递减分块填至剩余 <256B**（单次 14MB 在本机 Chromium 配额下仍容得下小写入，初版假阴性，已修）。
- command-notice 触发方式的**实证改道**（脚本注释内有原文说明）：原拟「配额满→Ctrl+S→附属记录失败」，源码核实 `canvasDocHost.remember` 与 `handleStore.setFileHandle` **均在内部 try/catch 静默吞掉配额错误**（两处注释明示「不阻断编辑主流程」），SAVE_METADATA_WARNING/onCommitError 在该路径不可达；改用真实可达失败路径——关系模式右键树边「切断并独立」对 7 级深链触发 planCutTreeEdge 的 MAX_NODE_DEPTH 拒绝。
- unconfirmed：TreeErrorBar（真实 FSA 扫描失败在内存盘不可复现；几何同源由单测钉死）。

---

## 5. 门禁原文与冻结基线逐数对照（本机 2026-09-24 顺序单跑）

| 门禁 | 冻结基线 | 本包实测 | 判定 |
|---|---|---|---|
| canvas 测试 | 58 文件 / 673 | **62 / 740**（+4 新文件：27+13+8+12=60；doc-index +4、file-manager-doc-index +3） | 新增，全绿 |
| react 测试 | 162 / 1778 | 162 / 1778 | 持平 |
| kernel 测试 | 75 / 708 | 75 / 708（本包未触 kernel） | 持平 |
| doc-index 专项 | 75 | 79（同文件 +4 标题接线例） | 新增 |
| lint warnings | 1530 | **1529**（629 文件，48 infos） | 只降不升 ✅ |
| depcruise | 604 modules / 1842 deps / 0 | **622 / 1903 / 0** | 0 violations ✅ |
| budget any/tsIgnore | 0/0 | 0/0 | 持平 |
| budget bang | 89（预算 90） | 89 | 持平（≤90） |
| budget asCast | 31 | 31 | 持平 |
| budget console | 4（测试注释口径计入） | 4（接管时 5：useFileTreeOps 注释里的 `console.error` 字面量被口径计入，已改写措辞） | 持平 |
| budget todo/defaultExport | 1/2 | 1/2 | 持平 |
| budget bigFiles | 4（>600） | 4：MindmapStage 3158、index.ts 813、MapView 2702、edgeRouting 1235 | 持平（FileManager 576、docIndex 599 均 <600） |
| build | exit 0 | **exit 0**，main-DQeEtSpE.js 532.77 kB | ✅ |

命令原文（均退出码 0，除负控 RED 外）：
- `pnpm vitest run`（apps/canvas、packages/react、packages/kernel 各自目录，顺序单跑）
- `pnpm gate:fast`：typecheck ✅ → depcruise `✔ no dependency violations found (622 modules, 1903 dependencies cruised)` → biome `Found 1529 warnings. Found 48 infos.` → budget `✅ 全部指标在预算内（债务未增长）`
- `pnpm build`：kernel/react/canvas 三个 build: Done，exit 0
- kernel flake：本轮单跑未遇（施工方 11 次中 0 次；如复核再遇按 R1 纪律记录文件+用例名连跑刻画）。

---

## 6. 预裁决「实证反驳」记录

| 预裁决 | 实证 | 新裁决/处置 |
|---|---|---|
| §3.1 归档 = moveFileSafe（复用互斥域/FileOpOutcome/冲突三选） | 代码实读：P0 的树内移动本就有**两条既有路径**——当前文档经 orchestration moveCurrent→`moveFileSafe`（四态/租约/三选），非当前文档的拖拽归位经 `workspace.moveFile`（read+write+remove 原始抛错 API，uniqueName 静默留两份）；`useFileManagerRouting` 注释明示「只有当前文档走租约，其他保持既有语义，既有用例有断言」 | 归档不新写编排：落点统一经**唯一路由点** dropIntoRouted，当前文档必达 moveFileSafe+三选（NC4 转红证明）；非当前文档沿用既有移动语义（与拖拽完全同路）。无新冲突策略、无第二写路径（archive 测试断言 removeFile=0、moveFile=1） |
| §3.6「错误类常驻、成功类自动消退」 | 四浮条改动前的真实消退：命令告警（错误色）本就是 4s 自动、规模提示（错误色）常驻、资产条（信息色）manual、预方向 2.5s 自动——tone 与消退并非一一对应 | 禁令解读为「**不得把既有的常驻条改成自动消退**」：四槽 dismiss 与改动前逐一相同（测试逐值钉死）；只统一「位置写在哪」，不统一「何时消退」。scale-notice/asset-notice 仍 manual |
| §5/§3.4「标题搜索消费既有 title」 | P0-D 只交付字段，生产零写入点，功能在真浏览器不可用（B3 首轮真浏览器实测 FAIL） | 属本切片必须补的最小生产接线（§6-6 无消费者反向情形）：从**已加载快照**取 H1，无新 I/O；补 7 个单测 + 真机复验通过 |
| （接管发现）onMoveFailed 已接线 | FileManager 调用点漏传，归档失败 unhandled rejection（Vitest Errors=1） | 已接线并由 NC-5 以 unhandled rejection 形态钉死判别力 |

---

## 7. 硬边界自查

- 文件夹移动 / 跨目录复制 / 批量操作：**零新增**（归档仅单文档）。
- 资产改名：零触碰；撤销：零新增（DS-13 归档替代语义不变）；自动重绑：仅复用当前文档既有 move 编排。
- scopeIdentity 判定链：零改动（rider-B 面板不 import 任何判定链符号）。
- 未挂任何 window 级树键监听（R1-3）；输入框让位在输入框自身按键层。
- 无文件推过 600：FileManager 576、docIndex 599（新增 title 字段以单行注释压入，未破线）；新逻辑全部落新模块（14 个新 src 文件）。
- 无 it.fails / 反转断言 / 宽泛 skip；负控反例不在默认门禁以常态失败形态存在（它们是生产中性化，断言始终正向）。
- 未修改他人用例换绿；未碰 kernel；未重开任何已收口契约（§6 收口判据 5）。
- backlog §6 明确排除项（上下文菜单键盘化/拖放提示条/筛选/命中计数/三个菜单项）：**未做、未登记为完成**，见 §8。

## 8. 未覆盖/未做清单（如实）

1. **未验证**：真实 OS IME 在重命名框内的组合键；触控拖拽归位；真实浏览器跨内核（仅 Chromium 1243）；真实 FSA 权限撤回/失联；离线/跨平台像素。
2. **未做（backlog §6 排除项，后续 backlog 登记）**：上下文菜单 Shift+F10/role=menu 键盘化；拖放提示条与 🚫 落点反馈；筛选（只看未保存/失联）；「N 项匹配」命中计数与 Esc 清空搜索；「移动到…/重新定位/在系统文件管理器中显示」三菜单项。
3. **既有观察（非本包引入，不改）**：保存附属记录失败的用户可见通道（SAVE_METADATA_WARNING/onCommitError）在现行实现下基本不可达——`canvasDocHost.remember` 与 `handleStore.setFileHandle` 都内部静默吞错；verify 脚本改走可达的命令层失败路径。是否要让元数据失败上浮属独立产品决策，留待主控。
4. TreeErrorBar 真实 FSA 扫描失败未在真机复现（内存盘恒成功）；几何同源由单测覆盖。
5. 真浏览器替身的局限：归档目录创建在非当前路径由替身模拟（生产侧 moveFileSafe 的 create:true 仅当前文档路径实测走到）；三选 UI 的完整人工点选在 X1 旅程中未展开（冲突不静默覆盖由 NC4 单测钉死）。

## 9. 产物索引

- 生产（27 文件，feat 提交 ed09c48）：新增 14 模块 + 修改 13 文件，清单见提交 stat。
- 测试/工具（8 文件，test 提交 c10bd88）：4 新测试文件、2 扩展测试文件、tools/verify-p1a-journey.mjs、tools/verify-fm-containers.mjs（均不进 CI）。
- 证据：
  - 负控 7 条 RED/GREEN：`evidence/2026-09-24-p1a-negative-controls/`（14 份日志）
  - 真浏览器 2 脚本 PASS：`evidence/2026-09-24-p1a-e2e/`（2 份日志）

**停止点**：回执提交即停。不进入 P1-B（图库发现与断图恢复），不做资产改名。
