# ② 二级环 · 内容收敛与画布接线 派遣计划（2026-09-12）

| 项 | 值 |
|---|---|
| 上游设计 | `docs/specs/2026-09-11-radial-phase3-design.md` §2（v1.8.2 更新：6 席 + 翻页 + 菜单分工 §2.2.3） |
| 前置 | 沙盒已实现：`/radial.html`（`ctx.sub` 开关 / 席位 4–6 / 展开延时 200–800ms）；纯逻辑 13 例 + 真浏览器三查已过 |
| 纪律 | 每任务：**先写失败测试 → 实现 → 全绿 → 单独提交**（TDD；单一职责，不混批） |
| 待批决策 | B4 翻页（推荐）/ B5 移除 4 项（推荐）/ 展开延时手感（默认 450ms） |

## 任务表

| # | 任务 | 产物 | 测试（先红后绿） | 验收 |
|---|---|---|---|---|
| T1 | 引擎：席位 `disabled` + 外圈**页** | `radialActions.ts`：`RadialSubItem.disabled`；`RadialState.subPage`；页内 `arrow/hover/click/confirm/alt-up` 全通；**未席「返回」= 回上一页**（页 0 = 回一级） | 纯逻辑 +6：灰显席不可高亮/不可提交；进页/回页；页内轮转环绕；`ctx.sub` 缺省零影响（回归） | react 套件绿 |
| T2 | 派生：`submenuItemsFor` | `contextMenuItems.ts` 新导出：静态顺序模板 + 条件过滤（转 `disabled`）+ 动态文案（枢纽/中心）+ 页定义（升为中心 → 方向页） | 一致性测试：① 6 席 id/顺序固定；② 条件态（根 / 中心 / 枢纽）下 `disabled` 正确；③ 文案动态（升级 ↔ 取消） | react 套件绿 |
| T3 | 渲染：页与灰显 | `radialSurface.tsx`：`SubRing` 支持 `disabled` 灰显（复用一级灰显色）+ 页切换复用 `.ring-in` 动效 | 渲染测试：灰显席 stroke 为灰；页切换后 items 更新 | react 套件绿 |
| T4 | 预览页：接派生 + 状态模拟 | `/radial.html`：席位来自 `submenuItemsFor`（stub controller）；新增「模拟：根 / 中心 / 枢纽」复选框看条件灰显与翻页 | `tools/verify-delete-subring.mjs` ③ 扩展：进二级 → 选「升为中心」→ 翻页 → 选方向（沙盒只回日志） | 脚本三查全绿 |
| T5 | 画布接线：`ctx.sub` + `sub:*` 动作 | **✅ 已完成（2026-09-12）**：`useRadialStage` 增 `getSubModel`（会话开始拉取派生模型）+ `ctx.sub` 喂状态机 + `sub:*` → 模型动作闭包（`sub:more-menu` 宿主播 `openMenu`）+ 「更多」停顿 450ms 下钻（`SUB_DWELL_MS`）+ `RadialStageOverlay` 渲染外圈/降透明/二级浮标；`MindmapStage` 以 `nodeMenuBags.ts` 构建**一份**动作袋（描述 / 笔记 / 中心）同时喂菜单与环 → 同一闭包 | canvas 集成测试 +12（下钻 / 零影响 / 轮转提交 / 翻页 / 灰显跳过与点击无效 / 悬停即时 / 末席兜底 / Esc·Backspace 收起 / 内圈降级 / 覆盖层渲染 / 停顿下钻 / 停顿不生效） | canvas 套件 **101** 绿；真浏览器 ④ 主画布实测「松键真建同级节点（25 → 26）」 |
| T6 | 菜单瘦身 + 深度优化 | **✅ 已完成（2026-09-12）**：① 阶段 1 移除 4 项（描述 / 笔记 / 枢纽 / 复制编号——环内等价入口）；② **方向/参数型收成「一行 + 子页」**：`ContextMenuItem.page` + 菜单内翻页（首行恒「‹ 返回」，`←`/`Backspace` 回页，Esc 一键关闭）；生长方向 / 出线长度 / 升为中心 各 1 行，**行内显示当前值** + 行尾 hint（Alt+方向 / 拖梁 / 环：更多）；③ 真浏览器实测补 **菜单视口钳制**（贴底右键菜单顶出屏幕 → 按实测尺寸收进视口） | 菜单回归：**28 → 12 行**（普通节点）；被移除项不再出现（跨模块不变量：环席位仍在）；子页 ✓ 标当前值；hint 文案；钳制用例 | react 套件绿（菜单 10 + 席位模型 11 例）+ 真浏览器 ⑤ |
| T7 | 文档与帮助 | **✅ 已完成（2026-09-12）**：`ShortcutHelpPanel` 追加「② 二级环席位」区块——**7 席标签从派生模型 `subRingPagesFor` 取**（与环/菜单同一份定义，改席位不会漏改帮助面板）+ 方向页说明；`keys.ts` 补 2 行（二级环展开 / 菜单翻页）；测试断言「面板标签与派生模型逐字一致」 | 帮助面板 +1 例（7 席 + 方向页 + 描述与模型一致）；真浏览器 ⑥：`?` 打开 → 7 席清单 → Esc 关闭 | react 套件绿；lint / 预算绿 |

## 交付顺序与检查点

`T1 → T2 → T3`（引擎与派生先行）→ **检查点 A：沙盒验手感（T4）** → `T5`（画布 ✅）→ `T6`（菜单瘦身 + 深度优化 ✅）→ `T7`（帮助面板补二级席位清单 + CHANGELOG）。
- 检查点 A 用户已确认（2026-09-12：席位定 **7 席**——内容收敛后追加「新建同级节点」为第 1 席、剪贴板席动态化；展开延时 450ms）
- T6 涉及能力入口移除（B5），执行前再确认一次

## 验收总纲

- **一级 4 项不动**（零回归）
- 二级 6 席：条件**灰显不抽席**（顺序/角度稳定）；**翻页**承载方向型动作；**兜底末席**恒可用
- 菜单 **28 → 24**（阶段 1），被移除 4 项全部有环内等价入口
- 真浏览器三查 + 纯逻辑 / 画布 / 菜单三套件全绿；`tsc` / lint / 预算绿
