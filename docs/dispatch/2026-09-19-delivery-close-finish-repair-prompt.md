# DELIVERY-CLOSE 定向返修启动 prompt

你负责 MindCanvas DELIVERY-CLOSE 深度收尾返修。唯一工作区 `<workspace>`。

先读 `docs/dispatch/2026-09-19-delivery-close-finish-review.md`，按 DF-R1–R4 逐项关闭；原深度计划仍适用，本复核对 Canvas 连线的限定处置优先。不要重做已认可的保存后 Undo、自由画布语义往返、主题导出成果。

先复现独立探针：

```powershell
node outputs/delivery-close-owner-review-20260919-01/audit.mjs
node outputs/delivery-close-owner-review-20260919-01/canvas-path-probe.mjs
```

audit 是只读检查，退出 0 不等于通过：具体检查 `candidate.drift`、`gateConfigs`、`handleProbe`；Canvas 探针正确期望当前应 exit 1。探针只写主控自己的证据文件，正式返修请复制到你独占目录后调整输出路径，保留保护性期望。禁止覆盖主控或执行者固化证据。

执行内容：

1. DF-R2：handle 创建时固定 scope、原始文件内容和目的地身份；跨夹具后旧 handle 仍读写旧文件，延迟 close 不串档。实际浏览器注入与 helper 测试使用同一实现。
2. DF-R3：补 0.26 的双向滞回、真实手势活动窗口 LOD 冻结/释放后收束、拖中心时另一岛与视口不误移动；邻域不到位不能 PASS，跨两阈值条件使用实际阈值。沿用已有正常矩阵，不靠更改正确预期过关。
3. DF-R4：这是已授权的必要最小产品修复。实现现有 Canvas 场景输出的树线/箭头路径；先正确期望红，再最小修复绿，浏览器检查实际连线区域与端点，省略绘制的负控必须命中。不要扩展为 DPR、图片缓存或后端重构。含中心/自由边优先 SVG 的保护不变。纠正影响范围是 layout 节点总数超过 50000，非视口可见计数。
4. DF-R1：所有相关文件定稿后建立新候选。修正隐藏配置排除，覆盖源码、测试、构建/门禁、工具/夹具和实际渲染资产；可采用完整清单，或明确固定 HEAD + 完整差异覆盖的可重建方案。立即逐项复算，新旧候选差异写清楚。文档完成后另外固化交付清单；保留旧 99 项 candidate 与 171 项 manifest，禁止覆盖修账。

先登记现有工作区与来源，不以旧 HEAD 新建空工作树丢失成果。RunId、输出目录、报告名称必须唯一；共享源码仅一个写入执行者。需要探针副本就放独占目录，改输出位置不改断言。

产品修复完成后，在最终候选上一次运行 gate/build/analyze，顺序运行发布矩阵、save-lifecycle、mode-guard。新增变化、失败或具体未决问题才补跑，不机械反复全量。每个负控记录固定集合、目标红项、连带红项与环境错误；环境异常不能算有效负控。

继续遵守不 commit/push/pull/rebase/reset/stash、不整树暂存、不发版、不提高预算、不覆盖他方文件、不启动图库/文件管理实现或第 4 项。只停止本轮服务。

交独占返修回执，逐项列 DF-R1–R4 的红→绿、差异、正常/负控、最终候选/清单与未覆盖限制。遇单项阻断继续完成独立可做部分；不得放松断言或自行降级主控关闭项。完成后停在回执，等待主控复核。
