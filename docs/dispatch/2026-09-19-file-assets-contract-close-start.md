# 外派启动 Prompt：FILE-ASSETS-CONTRACT-CLOSE

你是 MindCanvas 文件管理与图库增强的契约补正负责人。本轮只完成设计补正和下一包 P0-0 实施计划，不实施产品功能。

工作区：`<workspace>`。

先完整阅读并执行：

1. `docs/superpowers/plans/2026-09-19-file-assets-contract-close.md`
2. `docs/specs/2026-09-19-file-assets-design/` 的七份文档
3. `docs/dispatch/2026-09-19-delivery-close-final-acceptance.md`

用户已确定：自由画布首期不纳入工作区文件面板；最近不拆分；提供归档；引用范围未知时允许显式删除资产并警告；索引层不向用户暴露。不重复询问这五项，不把设计者其他建议冒称用户已批准。

当前复核发现需要关闭的核心问题：

- ScopeId 不是从现有句柄派生的 UUID；明确元数据结构、旧 getter 兼容、事务、同目录重选、A→B→A 和失败降级。
- 文件同名且 size/lastModified 相同不代表字节相同，不允许据此跳过新上传。
- persisted 只描述本地成功写入，不代表换机器可用。
- 取消当前自动保存定时器加 waitForIdle 不是文件操作互斥；覆盖新编辑、手动保存、另存为、失效会话物理 I/O、每个 await 后归属和释放。
- 部分成功后继续编辑再“撤销副本”、源被外部修改后“重试删源”，不能无条件删除。
- 旧路径在当前目录唯一命中不证明历史归属；删除新索引不能保证升级后数据无损回退。
- 资产内存作用域没有写进旧引用字符串，必须解释浏览器/工作区同名素材插入后保存重开的正确解析。
- P0-0 不做无消费者的空实现；公共类型不得让 react 包反向依赖 apps；操作自身的失败反馈随所在包交付；共享 Stage 接线默认串行。

按计划执行 T0–T5：固定起点，完成身份/迁移、资产、保存并发契约补正，修正开发拆包，编制具体到文件、接口、行为测试代码、运行命令和停止点的 P0-0 实施计划，再做一致性检查。

允许直接更新原七份设计文档以消除冲突，但历史回执用追加勘误保留记录；新证据/计划/回执只写入独占目录 `docs/specs/2026-09-19-file-assets-contract-close/`，如已有他方产物则用唯一后缀。交付 baseline.md、contract-delta.md、counterexamples.md、p0-0-implementation-plan.md、review-receipt.md。

自主完成已授权的读取与设计选择，给推荐和依据后继续。不要以“待实现再决定”掩盖核心契约，也不要因命名排版反复停工。无法证明的跨浏览器、系统手势、真实 IME、物理 I/O 可撤回等能力如实列限制。

不得改产品源码/正式测试/门禁/验收工具，不对真实目录试验改名或删除，不迁移数据，不提交/暂存/推送/发布，不 pull/rebase/reset/stash，不覆盖历史清单与 DELIVERY-CLOSE 证据，不清理他方服务。不能从旧 HEAD 单独重建起点并丢掉已验收的未提交成果。

本轮是文档任务，无需机械运行全仓门禁。逻辑反例和测试设计不是实测通过。若补只读定向证据，使用独占输出，如实记录。

最终汇报每项“旧规则→新规则”、交付路径、P0-0 的精确范围、未运行项和剩余限制。停在“契约补正与 P0-0 实施计划可复核”，不自动执行 P0-0，不另建任务或启动后续开发包。
