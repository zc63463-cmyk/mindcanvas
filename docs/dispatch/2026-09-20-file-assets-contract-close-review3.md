# FILE-ASSETS-CONTRACT-CLOSE v4 定向复核

日期：2026-09-20。范围：CR2-4A、CR2-4B、NC-7；不重开 CR2-1…3，不启动 P0-0。

结论：合法记录构造与 NC-7 的证据分层可以接受；CR2-4B 的正确断言已修复，但正常测试与负控运行仍未分开。完成下述一项执行编排修正和两处文案同步后，计划再作定向核对。此结论不是实现通过或发布批准。

## 1. 已接受部分

- CR2-4A：计划 1284–1288 行的 legalAppend 将旧条目转 dormant；并发正例先检查两次写入 ok，再检查三条记录、唯一 active 与对应身份。删除失败用例 668–675 行也先形成合法 dormant 记录，消除了在 delete 前被 invalid 拦截的问题。
- CR2-4B：expectThreeUpdates 已恢复同一正确期望；没有再把丢更新写成通过条件。这一修正保留。
- NC-7：1373–1397 行明确区分同步 mutate 返回值契约与真实事务生命周期；不再从 Promise.resolve 的微任务时序外推浏览器行为。本轮仅接受这一设计边界，没有实跑产品或浏览器生命周期验证。

## 2. 唯一剩余执行问题：NC-5 无条件负控被要求进入正式套件

位置：`docs/specs/2026-09-19-file-assets-contract-close/p0-0-implementation-plan.md:1328`，以及 1352、1354 行。

该负控在测试调用方无条件执行事务外 read、barrier、构造 next，随后调用 `writeWorkspaceRegistry(() => next, ...)`。回调忽略事务内最新 prev；两次 next 都来自同一旧快照，合法校验也不能阻止它覆盖前一次更新。

但 1352 行要求“两段代码加入 handle-store.test.ts”，1354 行要求“正确实现：正例与负控都通过”。两者与代码实际行为不一致。按当前文本实施会把一条必红用例带入正常门禁，不需要生产实现存在缺陷即可失败。

### 本轮实跑

独占证据目录：`outputs/file-assets-contract-review-20260920-03/`。

从 v4 原文提取 NC-5 代码块，保留 legalAppend、barrier、两个测试体与全部断言；仅以独立的最小事务模型提供 loadTwoInstances、数据库及断言适配。模型有两条实例 writeChain、一条共享事务队列、事务内最新读取、同步 mutate 和记录合法性检查。

| 子进程 | 写入模型 | 实际记录 | 断言 / 退出码 |
|---|---|---|---|
| positive | 正常事务内 RMW | a、b、c；唯一 active=c | PASS / 0 |
| negative | 同一正常模型，原文调用方预构造旧快照 | a、c；唯一 active=c | AssertionError / 1 |

两份记录均通过参考不变量检查。负控失败恰为缺失 b；不是非法记录、环境或导入失败。子进程真实退出码保存在 results.json，父级收集程序正常结束不等于负控退出 0。

复现：`node outputs/file-assets-contract-review-20260920-03/check-v4.cjs`。

证据边界：这是原文用例对最小事务模型的逻辑验证，非生产模块验证，非自制 IDB 夹具校准，非 TypeScript 类型检查，非浏览器测试。输入与原始代码块 SHA-256 保存在 results.json。

### 最小修正要求

1. 正常正式套件只加入正常调用方的并发回归用例，正确实现必须通过。
2. 当前 outOfTxAdd 演示移到独立负控副本或独立运行配置，明确不会被正常 gate/test 自动发现；保留 expectThreeUpdates 原样。单独执行预期 AssertionError、退出非零，标为“调用方旧快照逻辑反例”。
3. 若 NC-5 承担生产写路径的保护证明，实施时还须在隔离实现副本中移动读与构造的位置；保留正式调用方、两独立实例、各自 writeChain 和正确断言。barrier 仅提供受控交错，不能改保护性期望。正常副本退出 0，中性化副本因丢更新退出非零，两者分别留证。
4. 删除“当前两段代码原样进正式套件且正常时都通过”的矛盾表述，列明正常入口、逻辑反例入口、生产中性化入口及各自证据对象。不得使用反转断言、it.fails、catch 后报 PASS 或宽泛跳过来掩盖这个问题。

不要求为此修改产品 writer 以识别任意闭包中的旧数据，也不要求扩展产品功能。

## 3. 两处同步文案

- shared-contracts.md:97 与实施计划:290：“active 恰好一个”改成“activeScopeId 非 null 时，active 恰好一个且 ID 匹配”；null 分支保持零 active。当前文字要求所有条件同时成立，会与空库及 detach 合法状态冲突；用例已体现正确意图，本轮模型按该条件化意图验证。
- 实施计划:1204 仍写“三条”阴性对照，与 NC-1…NC-7 不符。更正标题即可，不以固定测试数量替代行为判据。

## 4. 下一次交回范围

仅补正上述执行编排和文案，保留已关闭项及原证据。可用独立脚本展示正常入口退出 0、负控入口断言失败退出非零，并如实说明是否已接生产模块。P0-0 尚未实施，不得把逻辑模型结果写成产品测试通过。

本轮仅新增本复核文档及独占证据目录，未修改交付方文档、产品源码、正式测试、门禁或旧证据。未运行全仓测试/typecheck/build；未暂存、提交、推送或发布。复核起点 HEAD 为 a2ce72b，工作树原有 131 条状态记录。
