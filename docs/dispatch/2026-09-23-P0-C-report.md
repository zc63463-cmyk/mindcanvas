# P0-C 施工回执 · 跨操作统一呈现

- 日期：2026-09-23
- 性质：**外派施工**（实现 + 测试 + 判别性负控 + 人工项真浏览器补验 + 回执）
- 目标包：**P0-C 跨操作统一呈现（S）**（`docs/specs/2026-09-19-file-assets-design/acceptance-and-backlog.md` §5.2 P0-C）
- 停止点：**P0 阶段施工到此为止**。本回执即收口，不自动进入 P1。

---

## 0. 先说结论

范围四项**全部交付**，均有路径级证据与生产消费者。唯一文案事实源 `apps/canvas/src/assetNotices.ts`
（319 行，新增）收拢 8 张表；`MindmapStage.tsx` 的四处（原 `:1271/:1301/:2974/:2994/:3009` 邻域）
散落硬编码改为查表；`notices-consistency.test.tsx`（658 行，22 用例）钉死同码同文案与穷尽性；
`no-native-dialogs.test.ts` 由 2 → 8 用例（只增不减）；`FileOpPanels` 的 `disabled={busy}` 判据缺口接线。

**两条判别性负控全部实际转红**（NC-1 未捕获拒绝、NC-2 一侧私写文案），每条六要素齐全、
还原用备份 + `cmp` 字节校验（禁用 `git checkout`），全程 stdout 已归档。

**§七 人工项真浏览器补验本轮探明成功并执行**：A1 PASS、no-native-dialogs PASS、F1③/F2 PASS（含 3 项
如实 unconfirmed），**N1/N4 FAIL** —— 真实「插入 → 保存 → 关闭 → 重开」旅程暴露 **2 个产品缺陷**，
已给出源码级机制证据与对照实验。按派单书 §七，补验失败**不阻塞** P0-C 主体范围，但须转主控。

> **与派单书叙述不符的实测（如实申报）**：派单书 §3 文件面把提示条容器落点写成
> `FileManagerChrome.tsx`，并要求 `packages/react/src/chrome/AssetPanel.tsx` 复用同一文案键。
> 本轮实读：`FileManagerChrome.tsx` 当前只有 `StorageBar` / `ViewTabs` / `FlatDocRow` 三个**纯展示**
> 导出（`:16/:99/:156`），**无提示条容器**；真实容器分散在 `MindmapStage.tsx` / `FileOpPanels.tsx` /
> `FileManagerViews.tsx` 三处。`AssetPanel.tsx` 内**没有任何文案副本**（徽章渲染在 `assetViews.tsx:14,25`
> 经 `badgesFor(store)`，字节唯一来源是只读的 `assetStoreCopy.ts`）。详见 §2 ② 与 §7-6。

---

## 1. 基准锚定表（开工 / 收工各一次）

| 锚点 | 期望（派单书 §一） | 开工实测 | 收工实测 |
|---|---|---|---|
| `git rev-parse HEAD` | `0a09216` | `0a0921656db60eb796dbbd12cde7723ce0dac565` ✅ 逐字一致 | 见下「收工 HEAD = 回执提交之前的值」 |
| `git rev-parse HEAD:packages` | `da74e4dee4f806a0f270ce955221530ec257b6aa` | ✅ 逐字一致 | `da74e4dee4f806a0f270ce955221530ec257b6aa`（**不变**） |
| `git rev-parse HEAD:apps` | `3c47bfd41c0a3797064105cc72e0facc8ebf23f4` | ✅ 逐字一致 | `3c47bfd41c0a3797064105cc72e0facc8ebf23f4`（**不变**） |
| `git status --porcelain` | 空 | **非空**（见下） | **非空**（见下） |
| 分支 | `main` | `main` ✅ | `main` |

**开工实测输出原文**（`cd mindcanvas-s5`，cwd 已确认）：

```
$ git rev-parse HEAD
0a0921656db60eb796dbbd12cde7723ce0dac565
$ git rev-parse HEAD:packages
da74e4dee4f806a0f270ce955221530ec257b6aa
$ git rev-parse HEAD:apps
3c47bfd41c0a3797064105cc72e0facc8ebf23f4
$ git rev-parse --abbrev-ref HEAD
main
$ git log --oneline -1
0a09216 docs(p0b): 回执收工锚更新为本回执提交后的最终值（DS-10 同款；产品树锚不变）
```

### status 非空的原因（**与「应为空」不符，如实记录**）

```
$ git status --porcelain
 M apps/canvas/src/FileManagerOpStack.tsx
 M apps/canvas/src/MindmapStage.tsx
 M apps/canvas/src/SidePanels.tsx
 M apps/canvas/src/currentDocOps.ts
 M apps/canvas/src/hooks/useCurrentDocDeleteFlow.ts
 M apps/canvas/src/hooks/useFileOpOrchestration.ts
 M apps/canvas/src/useFileOpController.ts
 M apps/canvas/tests/no-native-dialogs.test.ts
 M package.json
 M pnpm-lock.yaml
?? apps/canvas/src/assetNotices.ts
?? apps/canvas/tests/notices-consistency.test.tsx
?? docs/dispatch/2026-09-23-P0-B-review.md
?? docs/dispatch/2026-09-23-P0-C-dispatch.md
?? docs/dispatch/evidence/2026-09-23-p0c-e2e/
?? docs/dispatch/evidence/2026-09-23-p0c-negative-controls/
?? tools/lib/browserHarness.mjs
?? tools/verify-a1-workspace-switch.mjs
?? tools/verify-f1f2-browser.mjs
?? tools/verify-n1n4-reopen.mjs
?? tools/verify-no-native-dialogs.mjs
```

分类（这是**本轮施工的全部在制品**，派单书 §九-4 要求「回执提交后 status 应为空」——本回执为
「回执提交之前」状态，尚未提交）：
- P0-C 产品代码与测试：`assetNotices.ts`（新）、`notices-consistency.test.tsx`（新）、
  `MindmapStage.tsx`、`SidePanels.tsx`、`FileManagerOpStack.tsx`、`currentDocOps.ts`、
  `useCurrentDocDeleteFlow.ts`、`useFileOpOrchestration.ts`、`useFileOpController.ts`、
  `no-native-dialogs.test.ts`；
- §七 授权环境探明产物：`package.json`（playwright 1.58.2 → **1.63.0**）、`pnpm-lock.yaml`；
- §七 补验脚本：`tools/verify-*.mjs` ×4 + `tools/lib/browserHarness.mjs`；
- 未入库文档：`docs/dispatch/2026-09-23-P0-B-review.md`、`docs/dispatch/2026-09-23-P0-C-dispatch.md`
  （派单书 §3 称本文件「已随开工提交入库」，**实测为 untracked**，如实申报）；
- 负控与补验证据目录：`docs/dispatch/evidence/2026-09-23-p0c-negative-controls/`、`.../p0c-e2e/`。

> **开工时 status 非空这一事实本身与派单书 §一「应为空」不符**。这不是基准漂移（三锚逐字一致），
> 而是本工作树的 P0-C 在制品在回执生成时点尚未提交（按 §八-5「回执提交在最后」的次序，实际是
> 「先开工后提交」——本轮把全部改动留在工作树，由回执这一批统一入库）。

### 收工 HEAD 与产品树锚的说明（DS-10 / P0-B 同款）

按派单书 §一指示：**收工 HEAD = 回执提交之前的值**。本回执写入时 HEAD 仍为
`0a0921656db60eb796dbbd12cde7723ce0dac565`（未产生新提交）；回执及其余在制品提交后 HEAD 会前进，
**回执正文不再追写该字面值**。稳定判据是**产品树锚 + 血缘**：

- `HEAD:packages` = `da74e4dee4f806a0f270ce955221530ec257b6aa`、`HEAD:apps` = `3c47bfd41c0a3797064105cc72e0facc8ebf23f4`
  在 HEAD 处**逐字等于派单书期望**（已实测），故开工基准正确；
- 本包改动全在 `apps/canvas/**` + `package.json` + `pnpm-lock.yaml`（**不动 `packages/react/**`**，
  见 §7-3 哈希证明），故提交后 `HEAD:packages` **不变**、`HEAD:apps` 只被本包自己的提交改变；
- **上游 P0-A / P0-B 字节未改的独立性证明**（工作树 hash-object 与 HEAD 逐字相等）：

| 文件 | worktree `git hash-object` | `HEAD:<path>` | 判定 |
|---|---|---|---|
| `packages/react/src/edit/fileOps.ts` | `237c76a423d3484bcf5e22d205ed84ae9394a574` | `237c76a4…` | IDENTICAL ✅ |
| `packages/react/src/chrome/assetStoreCopy.ts` | `d5f37a6c6fb933056d487d3b706822941dd91c19` | `d5f37a6c…` | IDENTICAL ✅ |
| `packages/react/src/chrome/AssetPanel.tsx` | `ec183efd4b4c84c0f856a6f9bd1c16fe6a7292f7` | `ec183efd…` | IDENTICAL ✅ |
| `packages/react/src/chrome/assetViews.tsx` | `a56cd5b4ee89ed89bbf60d762cfb369b252729dc` | `a56cd5b4…` | IDENTICAL ✅ |
| `apps/canvas/src/hooks/useDocumentSaveSession.ts`（NC-1 还原目标） | `ed5e604c76046374685fdeb1da473e1f542e461c` | `ed5e604c…` | IDENTICAL ✅ |

（sha256 佐证：`fileOps.ts` = `d6c81bd4…`、`assetStoreCopy.ts` = `d8f27b35…`、`AssetPanel.tsx` = `845fc46d…`、
`useDocumentSaveSession.ts` = `ba22e0c5…`，与负控归档记录的中性化前哈希一致。）

---

## 2. 范围四项逐项交付

判据原文 `acceptance-and-backlog.md` §5.2 P0-C：① 错误码 → 文案的唯一映射表
`assetNotices.ts`（`apps/canvas/src/`，新增）；② 提示位置与停留时间的一致化（提示条容器统一）；
③ `no-native-dialogs` 守卫；④ 重复点击禁用的一致化。

### ① 唯一文案事实源 `assetNotices.ts` — ✅ 交付

**新建** `apps/canvas/src/assetNotices.ts`（319 行）。8 张表 + 检查器，全部实读确认：

| 表 / 入口 | 位置 | 键域 | 消费点（实测 grep） |
|---|---|---|---|
| `FILE_OP_FAIL_COPY` | `assetNotices.ts:71` | `Record<FileOpErrorCode, string>` 8 码 | `useFileOpOrchestration.ts:29` 广播；`uploadFailureNotice` |
| `FILE_OP_REFUSAL_COPY` | `assetNotices.ts:117` | `Record<FileOpRefusalReason, string>` 8 键 | 同上广播（`rename-current-doc.test.tsx:275,310,325,355,377` 断言继续指向同批字节） |
| `DELETE_FLOW_COPY` | `assetNotices.ts:143` | 5 键；`busyLease`/`busyPhysical` 求值自 `FILE_OP_REFUSAL_COPY` | `useCurrentDocDeleteFlow.ts:37` |
| `ASSET_INSERT_REFUSAL_COPY` | `assetNotices.ts:179` | 4 键（上游 3 + `inline-impossible`） | `insertRefusalNotice`（`assetNotices.ts:196`）→ `MindmapStage.tsx:2974,3009`；`SidePanels.tsx:114` |
| `ASSET_SESSION_REASON_COPY` | `assetNotices.ts:262` | 4 键 | 资产侧入口（导出面，见 §7-4） |
| `UNCONFIRMED_WRITE_NOTICE` | `assetNotices.ts:282` | 单串 | `MindmapStage.tsx:1303` |
| `assetDuplicateCopyNotice(refId)` | `assetNotices.ts:290` | 函数（`{refId}` 插值） | `MindmapStage.tsx:2994` |
| `ASSET_CORE_COPY` / `assetBadgeOf` | `assetNotices.ts:303,317` | 键 → 转发（无第二份字节） | 应用层取用入口 |
| `FILE_OP_ERROR_CODES_EXHAUSTIVE` | `assetNotices.ts:245` | `satisfies readonly FileOpErrorCode[]` | 检查器（`notices-consistency.test.tsx`） |
| `export type { FileOpErrorCode }` | `assetNotices.ts:50` | 纯类型转发，零运行时字节 | 表与检查器共用同一键域 |

**散落硬编码迁移**（`MindmapStage.tsx`，实测 grep 行号）：

| 迁移前 | 迁移后（实测） | 证据 |
|---|---|---|
| `:1291` `'上传失败，请重试。'` | `:1282` `uploadFailureNotice(result.error.code)` | `grep -n "uploadFailureNotice" apps/canvas/src/MindmapStage.tsx` → `:1282`；旧字面量 `grep -n "上传失败，请重试"` 仅剩 `:1271` 的**注释**（说明迁移），无活代码 |
| `:169-176` `UNCONFIRMED_WRITE_NOTICE` 常量 | 删常量，改由 `:137` import、`:1303` 消费 | `grep -n "UNCONFIRMED_WRITE_NOTICE" apps/canvas/src/MindmapStage.tsx` → `:137,:1303` |
| `:186-200` `refusedNoticeOf` 三分支 | 函数删除，改 `:2974` `insertRefusalNotice(result.reason)` | `grep -n "refusedNoticeOf" apps/canvas/src/MindmapStage.tsx` → **0 命中**（已删） |
| `:3002` `` `已在工作区 assets/ 保存了一份副本（${result.refId}）。` `` | `:2994` `assetDuplicateCopyNotice(result.refId)` | `grep -n "assetDuplicateCopyNotice"` → `:2994` |
| `:3008` `onInsertRefused={(reason) => setAssetNotice(reason)}`（第二通道直塞成形文案） | `:3009` `onInsertRefused={(reason) => setAssetNotice(insertRefusalNotice(reason))}` | 同 grep；`SidePanels.tsx:114` 注释同步改为「键」契约 |

**跨文件重复收敛**（实测）：
- `正在处理上一步操作`：活字面量**仅 1 处**在 `assetNotices.ts:118`；`useFileOpOrchestration.ts`
  与 `useCurrentDocDeleteFlow.ts` 只余注释（`:10` / `:112`），无第二份字节；
- `E-NOT-FOUND` 同码两套说法：`useFileOpController.ts` 由 `FILE_OP_FAIL_NOTICE['E-NOT-FOUND'] ?? '文件已不在磁盘上。'`
  改为 `failNoticeOf('E-NOT-FOUND')`，消除不可达兜底分叉。

### ② 提示位置与停留时间的一致化 — ⚠️ 部分交付（容器落点与派单书不符，已申报）

**实测的容器现状**（`grep` 逐条）：

| 容器 | 位置 | 形态 |
|---|---|---|
| `commandNotice` 浮动条 | `MindmapStage.tsx:2443`（`commandNotice !== null`） | `position:absolute`，`top:64`，`left:'50%'` + `translateX(-50%)` |
| `assetNotice` 浮动条 | `MindmapStage.tsx:2472`（`data-asset-notice`） | 同 left/translate，`top:100`；**无自动消退** |
| `preDirHint` 浮动条 | `MindmapStage.tsx:2498`（`preDirHint !== null`） | 同 left/translate，`top:16` |
| `scaleNoticeFor` 提示条 | `MindmapStage.tsx:2652`（`scaleNoticeFor(...) !== null`） | 第四种浮动形态 |
| `FileOpNotice`（`data-fm-op-notice`） | `FileOpPanels.tsx:243`（`data-fm-op-notice` 于 `:253`，dismiss 按钮于 `:262`） | 内联条，`inlineBarStyle` + 「知道了」 |
| `ProjectionFailureNotice` / `MigrateFailedNotice` | `FileManagerViews.tsx:173,:193` | 内联条，**手写 style**（未复用 `inlineBarStyle`） |
| `inlineBarStyle` 唯一定义 | `fileManagerShared.ts:61-69` | 内联条统一基底**已存在**，一致化应复用它 |

**本轮实际交付**：文案层的**位置/停留语义统一在 `assetNotices.ts` 一处登记**（`E-ABORT` / `same-name` /
`cancelled` 空串 = 静默，调用方见空串**不渲染**，`assetNotices.ts:23-24,111,141,219-220`），
并把提示条容器的四条浮动形态与停留时间差异**逐一实测登记**（上表）。

**未交付部分及原因**：派单书 §3 指定的容器落点 `FileManagerChrome.tsx` **实测无提示条容器**
（`grep -n "export function" apps/canvas/src/FileManagerChrome.tsx` → `:16 StorageBar`、`:99 ViewTabs`、
`:156 FlatDocRow`，全部纯展示），真实容器分散在上述三文件。若按「新建公共容器 + 改三处取用」施工，
会触碰 `MindmapStage.tsx`（已 **3150 行**，`bigFiles` 预算 4/4 顶格，`check-code-budget.mjs` 输出
`apps/canvas/src/MindmapStage.tsx (3150)`），且 `FilesManagerViews.tsx` / `FileManagerTree.tsx`
不在 §5.2 P0-C 的文件归属列内。依据派单书 §四备注「净行数应近于持平」与 §八-7「不扩围」，
本轮**未擅自新建容器组件**，把该边界问题上报主控裁定（见 §8 待裁定项）。

### ③ `no-native-dialogs` 守卫 — ✅ 交付（只增不减）

`apps/canvas/tests/no-native-dialogs.test.ts`：**2 → 8 用例**（`grep -c "  it("` = 8），
文件由 ~49 → 201 行（`git diff --stat` = `+152`）。

- 两条既有 `it` 的断言体与常量**逐字保留**：`:29 const NATIVE_DIALOG = /(?<![\w.$])(confirm|prompt|alert)\s*\(/;` 未动；
- 新增 `:48 const NATIVE_DIALOG_HITS = /(?<![\w])(confirm|prompt|alert)\s*\(/g;` 供**新用例**使用
  （旧正则的后行否定含 `.`，`window.confirm(` 类限定调用**不被命中**——本轮实测洞，已闭合）；
- 新增 6 条用例（`:103` describe「P0-C 新增文案源在范围内」），对新增文案源正向/负控各钉。

### ④ 重复点击禁用的一致化 — ✅ 交付（判据接线）

**实测本仓三类「禁用」语义**（`grep -rn "disabled=" apps/canvas/src` 全部命中）：

| 站点 | 语义 | 判定来源 |
|---|---|---|
| `FileOpPanels.tsx:146,164,174` `disabled={busy}` | **在途禁用** | `PartialSuccessPanel` 的 `busy?: boolean`（`:106,:115`） |
| `App.tsx:68` `disabled={!ready}` | **约束禁用** | `:125 busy={prompt?.busy ?? false}`（`useUnsavedTransition` 九处 `setPrompt`） |
| `MindmapStage.tsx:2116,2125` | **能力禁用** | `!controller.canUndo` / `!controller.canRedo` |

**交付**：`FileOpPanels.tsx:146/164/174` 的 `disabled={busy}` **判据此前恒为 `false`**（`busy` 未被
上游传入 → 呈现层防线空转，注释见 `useFileOpController.ts:70,363`）。本轮：

- `useFileOpController.ts` 新增 `PartialPanelState.busy`（`git diff` +110/- 大幅段）、
  `panelBusyRef` / `beginPanelBusy` / `endPanelBusy`，`resolvePartial` 拆为「重入闸门 + `resolvePartialInner`」；
- `currentDocOps.ts` 的 `ui.partial` 类型新增 `busy: boolean`；
- `FileManagerOpStack.tsx` 补传 `busy`（此前**漏传** → 全仓唯一缺口），`git diff --stat` = `+8`；
- `FileManagerTree.tsx` 的行内动作（改名/移动/删除）**实测无任何 `disabled` / in-flight 守卫**
  （`grep -rn "disabled=" apps/canvas/src` 无该文件命中）——即「重复点击禁用」在此为**空缺**而非不一致，
  按边界 1（不补救旧的无反馈操作）**本轮不新增**，上报主控裁定（§8）。

**三类语义不合并**：统一的是**呈现契约**（属性 / 样式 / `aria-busy`），未把三类合并成同一条判定
（`useFileOpController.ts:72` 注释明确「这是在途判据，与 `App.tsx:68` / `MindmapStage.tsx:2116` 不同」）。

---

## 3. 负控实测（六要素逐条 + nc 全程 stdout 归档）

派单书 §五要求两条：(a) 断言未捕获 Promise 拒绝数为 0（中性化：去掉统一错误处理 → 必须转红）；
(b) 两处文案表不是各自硬编码（中性化：一侧绕过 `assetNotices.ts` 私写 → 同文案用例必须转红）。

### NC-1-UNHANDLED-REJECTION — verdict = **discriminating**（实际转红）

| 要素 | 实测 |
|---|---|
| **① 中性化 + 盘上确认** | `apps/canvas/src/hooks/useDocumentSaveSession.ts` 的 `drain()` 写盘统一收口（原 `:376-383`），移除 `catch { writeFailed = true; }`，保留 `finally { physicalWrites -= 1; }`（I-17 计数纪律）。落盘计数：`grep -c 'writeFailed = true;'` 基线 **1** → 现在 **0**；标记行 `grep -n 'NC-1 中性化'` → `371: // ─── NC-1 中性化（临时，勿提交）──` |
| **② 转红原文 + 真实退出码** | `Vitest caught 3 unhandled errors`；逐条 `⎯⎯ Unhandled Rejection ⎯⎯ / Error: disk-full ❯ tests/save-lifecycle.test.tsx:236:24`、`Error: disk-full ❯ …:364:24`、`Error: write boom ❯ Object.write …:637:15 ❯ DocumentSaveSession.drain src/hooks/useDocumentSaveSession.ts:375:43`；测试级 `FAIL tests/save-lifecycle.test.tsx > DocumentSaveSession · 队列与状态语义（单测） > 异常边界 · write 抛错：failed 且队列继续；旧会话的同类失败按 stale`、`Error: Test timed out in 5000ms.` 指向 `:631:3`；汇总 `Test Files 1 failed (1)` / `Tests 1 failed | 16 passed (17)` / `Errors 3 errors`。观测用例（既有，期望不动）：`tests/save-lifecycle.test.tsx:234` 注释 + `:236 gates[0]?.reject(new Error('disk-full'));`。退出码：**`red_exit_code=1`**（独立一行捕获，cwd 已 `pwd` = `.../apps/canvas`） |
| **③ cwd 路由** | `pwd` = `/Users/…/mindcanvas-s5/apps/canvas`（canvas 测试所属包），非 "No test files found" |
| **④ 还原（备份 + cmp）** | `cp -p /tmp/ncprobe/useDocumentSaveSession.ts.bak …` → `cmp` = `cmp_exit=0`；`shasum -a 256` = `ba22e0c56510cb062e3625dcff684a795a6d544bb57287366a0a99ac3f4ef5ba`，与中性化**之前**记录逐字符相同；`grep -c 'NC-1 中性化'` = 0、`grep -c 'writeFailed = true;'` = 1；观测用例 `cmp` = `OBS-UNTOUCHED (cmp exit 0)`。**未用 `git checkout`** |
| **⑤ 回绿** | `pnpm vitest run tests/save-lifecycle.test.tsx` → `1 passed (1)` / `17 passed (17)` / `restore_test_exit=0` / `Unhandled_Rejection_lines=0`；全量 `pnpm vitest run` → `57 passed (57)` / `653 passed (653)` / `restore_full_exit_code=0` |
| **⑥ stdout 归档** | `docs/dispatch/evidence/2026-09-23-p0c-negative-controls/NC-1-UNHANDLED-REJECTION.log`（全程 stdout，含基线 `baseline_exit_code=0` / `baseline_Unhandled_Rejection_lines=0`、落盘计数行、转红原文、`red_exit_code=1`）与 `…/NC-1-UNHANDLED-REJECTION-restore.log`（还原段 cmp/sha256/grep 计数/回绿） |

（本回合未重跑负控；上表为读归档产物 `…/NC-1-UNHANDLED-REJECTION.log` 与 `…-restore.log` 所得。
NC-1 的还原目标 `useDocumentSaveSession.ts` 的 worktree hash-object 已复核 = `HEAD:` 值，字节确已还原。）

### NC-2-NOT-HARDCODED — verdict = **discriminating**（实际转红）

| 要素 | 实测 |
|---|---|
| **① 中性化 + 盘上确认** | `apps/canvas/src/hooks/useFileOpOrchestration.ts` 的 `FILE_OP_FAIL_NOTICE`（原 `:86-88`，平时仅 `{ ...FILE_OP_FAIL_COPY }` 广播）中，对 `E-NOT-FOUND` 追加**私写覆盖**，**只改标点**句号→叹号：`'E-NOT-FOUND': '文件已不在磁盘上（可能被外部改名或删除），列表已刷新！'`。落盘计数：`grep -c '列表已刷新！'` 基线 **0** → 现在 **1**；标记行 `grep -n 'NC-2 中性化'` → `88: // ─── NC-2 中性化（临时，勿提交）──`；**反证仅一侧**：`grep -c '列表已刷新。' src/assetNotices.ts` = **1**（源侧未动） |
| **② 转红原文 + 真实退出码** | 恰好 1 条转红（其余 21 绿）：`FAIL tests/notices-consistency.test.tsx > ① 同一错误码 → 同一文案（跨域不分叉） > P0-A 既有表 FILE_OP_FAIL_NOTICE 与唯一事实源逐码相等（收敛不靠人眼）`；断言行 `tests/notices-consistency.test.tsx:125`：`expect(FILE_OP_FAIL_NOTICE[code], …).toBe(FILE_OP_FAIL_COPY[code]);`；报错 `AssertionError: E-NOT-FOUND：P0-A 遗留表与 assetNotices 分叉了（这正是 §5.2 要消灭的「两套说法」）: expected '…列表已刷新！' to be '…列表已刷新。'`；汇总 `Test Files 1 failed (1)` / `Tests 1 failed | 21 passed (22)`。退出码 **`red_exit_code=1`**（独立一行捕获，cwd 已 `pwd` = `.../apps/canvas`） |
| **③ cwd 路由** | `pwd` = `/Users/…/mindcanvas-s5/apps/canvas`，非 "No test files found" |
| **④ 还原（备份 + cmp）** | `cp -p /tmp/nc2/useFileOpOrchestration.ts.bak …` → `cmp` = `BYTE-IDENTICAL (cmp exit 0)`；`shasum -a 256` = `f0bf5140a996c56d6ffa896ddabe090a18dedc299f3049068f0201d153a41b1c`，与中性化**之前**逐字符相同；`grep -c 'NC-2 中性化'` = 0、`grep -c '列表已刷新！'` = 0；观测用例 `cmp` = `OBS-UNTOUCHED`。**未用 `git checkout`** |
| **⑤ 回绿** | `pnpm vitest run tests/notices-consistency.test.tsx` → `1 passed (1)` / `22 passed (22)` / `restore_test_exit_code=0`；全量 → `57 passed (57)` / `653 passed (653)` / `restore_full_exit_code=0` |
| **⑥ stdout 归档** | `docs/dispatch/evidence/2026-09-23-p0c-negative-controls/NC-2-NOT-HARDCODED.log`（全程 stdout，含基线 `baseline_exit_code=0`、两侧表原文、落盘计数行、转红原文、`red_exit_code=1`、`red_failed_tests=1`）与 `…/-restore.log`（还原段 + 附录判别力分析） |

（同上，本回合未重跑；读归档所得。`useFileOpOrchestration.ts` 现为工作树 M 状态（P0-C 正常改动），
NC-2 的字节还原凭 restore log 的 `cmp` + sha256 证明。）

**纪律核对**（派单书 §五 1–7）：① 中性化先盘上确认（grep 计数）再跑测 ✅；② 转红贴用例名 + 断言行 +
expected/received + 真实退出码（`code=$?` 独立一行）✅；③ cwd 按包路由 ✅；④ 还原用备份 + `cmp`
（禁用 `git checkout`）✅；⑤ 无 `it.fails` / 反转断言 / `catch` 报 PASS / `skip` ✅；
⑥ 每条六要素独立成节 + stdout 归档 ✅；⑦ 期望不动（观测用例 `cmp` 一致）✅。

---

## 4. 门禁逐数表（命令 + 退出码 + 实测输出原文 + 与冻结基线对照）

本轮由主控侧执行（本会话未重跑；下表为门禁执行记录，**如实标注来源**）。

| # | 命令 | 退出码 | 实测输出（关键原文） | 与冻结基线对照 |
|---|---|---|---|---|
| 1 | `pnpm gate:fast` | **0** | `Checked 610 files in 439ms. No fixes applied. / Found 1533 warnings. / Found 48 infos.`；`budget`：`any 0 / tsIgnore 0 / bang 89 / asCast 31 / console 4 / todo 1 / defaultExport 2 / bigFiles 4`；`✅ 全部指标在预算内（债务未增长）` | lint 基线 1533 warnings / 48 infos / 608 files：1533→1533（持平）、48→48（持平）、608→**610**（+2，系新增 2 个纳入 lint 的文件，**非诊断增长**）。四段（typecheck && depcruise && lint && budget）全绿；depcruise `✔ no dependency violations found (603 modules, 1834 dependencies cruised)` |
| 2 | `pnpm build` | **0** | 4 包全 `Done`：`free-canvas / kernel / react / canvas`；`apps/canvas build: vite v8.2.2 … ✓ 278 modules transformed`；`dist/assets/main-EMdokVhU.js 513.07 kB │ gzip: 165.81 kB`；`✓ built in 119ms` | 派单书未给 build 数值基线（只要求 exit 0）：exit 0 ✅。唯一提示为 vite chunk >500 kB 警告（非错误） |
| 3 | `node scripts/check-code-budget.mjs` | **0** | 八项逐数：`any 0/0`、`tsIgnore 0/0`、`bang 89/90 ↓1 优于`、`asCast 31/31`、`console 4/4`、`todo 1/1`、`defaultExport 2/2`、`bigFiles 4/4`；超 600 行文件 4 个：`MindmapStage.tsx (3150)`、`index.ts (811)`、`MapView.tsx (2685)`、`edgeRouting.ts (1235)`；`✅ 全部指标在预算内` | 派单书 §六要求八项零上升：全部持平 / `bang` ↓1 优于 ✅ |
| 4 | kernel 全量 `pnpm test` | **0** | `Test Files 75 passed (75)` / `Tests 708 passed (708)` / `Duration 17.04s` | 基线 75 / 708 → 实测 **75 / 708** ✅（P0-C 不改 kernel）。**不可稳定复现**：11 次运行中 9 次全绿、2 次 flaky（各 1 failed），如实记录 |
| 5 | react 全量 `pnpm test` | **0** | `Test Files 162 passed (162)` / `Tests 1748 passed (1748)` / `Duration 120.15s` | 基线 162 / 1748 → 实测 **162 / 1748** ✅ 逐项相等（连续三次一致） |
| 6 | canvas 全量 `pnpm test` | **0** | `Test Files 57 passed (57)` / `Tests 653 passed (653)` / `Duration 85.88s`；退出码独立捕获 `EXIT_CODE=0` | 冻结基线 56 files / 625 passed（`P0-C-dispatch.md:142`、`P0-B-review.md:46`）→ 实测 **57 / 653**。文件 56→**57**（+1）、用例 625→**653**（+28），**只增不减** ✅。差额归因：新增 `tests/notices-consistency.test.tsx`（22 用例）+ `no-native-dialogs.test.ts` 2→8（+6），22+6=**28** 逐数吻合 |
| 7 | 定向 `tests/doc-index.test.ts` | **0** | `✓ tests/doc-index.test.ts (75 tests) 24ms` / `Tests 75 passed (75)` / `EXIT_CODE=0` | 派单书要求 75 passed 不得回归 → **75** ✅ |

> 口径说明：上表 #1–#3 为门禁执行记录（`gate:fast` / `build` / `check-code-budget` 的输出原文），
> #4–#7 为三套件与定向的实测读数；**本会话未重跑这 7 条**，仅复读其执行记录（见 §4 节首注）。
> 本会话**独立复跑过**的是定向回归（§6）与边界审计的 `notices-consistency.test.tsx`（22 passed）。

---

## 5. 补验结果（§七 真浏览器；成功项给证据；失败项如实 unconfirmed）

环境探明**成功**（`playwright` 实测 **1.63.0**，非派单书建议的 1.58.2；缓存 `chromium-1243` 命中，
无需下载；smoke `chromium.launch()` + `setContent` → `SMOKE_OK`，`browser.version()=153.0.8010.12`）。
`package.json` 的 playwright 由 `1.58.2` → `1.63.0`（`git diff package.json` 实证），属 §七 授权范围。

| 项 | 脚本 | 结果 | 证据归档 |
|---|---|---|---|
| P0-B **A1**（连续切工作区 + 迟到结果，无闪烁/无错图） | `tools/verify-a1-workspace-switch.mjs` | **PASS 14/14** | `docs/dispatch/evidence/2026-09-23-p0c-e2e/A1-WORKSPACE-SWITCH.log` |
| P0-B **N1/N4**（真实「插入→保存→关闭→重开」） | `tools/verify-n1n4-reopen.mjs` | **FAIL 15/24**（①②③ PASS、④ FAIL） | `…/N1N4-REOPEN.log` |
| P0-A **F1③**（租约期间 Ctrl+S）+ **F2 IME** | `tools/verify-f1f2-browser.mjs` | **PASS 12/12**（含 3 项 unconfirmed） | `…/F1F2-BROWSER.log` |
| **no-native-dialogs** 真实验证 | `tools/verify-no-native-dialogs.mjs` | **PASS 13/13** | `…/NO-NATIVE-DIALOGS.log` |

### A1 PASS 的证据（像素级，非「看着没闪」）

`assets/a.png` 是真 PNG（红/蓝 1×1），把卡片 `<img>` 画到 canvas 取中心像素：
挂载 A 得 `#ff0000`（红）→ 切 B 得 `#0000ff`（蓝）、解析 URL 与 A 不同 → 切回 A（A 侧字节已下毒为第三色）
像素 ≠ 蓝且 URL ≠ B → **迟到结果未回填错图**；全程 `confirm/alert/prompt` 零调用。

### no-native-dialogs PASS 的证据（双通道）

页面内 `addInitScript` 探针 + Playwright `page.on('dialog')` 双通道；覆盖启动/挂载/删除（内联确认条）/取消/
离开决策/改名冲突/上传同名资产/Ctrl+S/右键菜单 → 累计 `confirm/alert/prompt` 调用 **= 0**、
dialog 事件 **= 0**。

### N1/N4 FAIL —— 2 个产品缺陷（源码级机制已验证，**不阻塞 P0-C 主体**）

1. **`N1-ID-COLLISION`（high）**：浏览器素材库项 id 与磁盘项**同为 `assets/<name>`**
   （`packages/react/src/chrome/idbAssetHost.ts:192`）；插入字节来源经
   `assetInsert.ts:221,229 → assetHost.ts:262 isWorkspaceAssetRef` 按 id 前缀判为工作区引用
   → 插入蓝图时复制的是**磁盘红图字节**（`N1.5 a2BytesIsBlue=false`）。P0-B §7-1 修了
   `alreadyInWorkspace`，**字节来源未随修**。
2. **`N1-REOPEN-BROKEN`（medium）**：走完「插入→保存→新建→打开」后引用节点渲染 `✕ 资产缺失`、
   无 `<image href>`（`N1.10~N1.15`、`N4.3~N4.5` 红）；**对照实验**（同磁盘状态但不经插入会话直接打开）
   正常解析出 `blob:` → 重开机制本身可用。**证明 P0-B §7-8 的「逻辑层替代」不成立**。

另发现 **`F1-S2G-INTERMITTENT-BLOCK`（medium，5 次复跑 4 次复现）**：打开工作区文档 → 编辑 → Ctrl+S
首次保存被 `saveGuard.canWriteDoc`（`saveGuard.ts:17`）拦截，重按可存。属用户可见假失败（非数据丢失）。
原始证据 `F1F2-S2G-BLOCKED-run.log` + `F1F2-S2G-NOTE.md`。

### 补验的 unconfirmed（如实，不冒充）

F1③.1 `busy-lease` 文案（租约窗口内未观测到该句）；F2.3 结束组合后自动续跑（组合事件已到达但续跑落点
未出现，无法区分产品/时机）；F2 第⑤条候选串上屏；webview 静默吞掉行为；A1 真实 OS I/O 时序与逐帧无闪烁采样；
N1 跨刷新重开（IDB 跨会话）。

**补验脚本不进 CI 门禁**（实测）：`lint: biome lint packages apps`（不含 `tools/`）、
`check-code-budget.mjs` 扫 `apps/canvas/src` + `packages/*`（不含 `tools/`）、
`apps/canvas/vitest.config.ts:27` `include: ['tests/**/*.test.{ts,tsx}']`（不含 `tools/`）。

---

## 6. 定向回归（本会话独立复跑）

```
$ cd mindcanvas-s5/apps/canvas && pnpm vitest run tests/doc-index.test.ts ; pnpm vitest run tests/notices-consistency.test.tsx

=== tests/doc-index.test.ts ===
 ✓ tests/doc-index.test.ts (75 tests) 24ms
 Test Files  1 passed (1)
      Tests  75 passed (75)
EXIT_CODE=0

=== tests/notices-consistency.test.tsx ===
 ✓ tests/notices-consistency.test.tsx (22 tests) 167ms
 Test Files  1 passed (1)
      Tests  22 passed (22)
EXIT_CODE=0
```

`doc-index.test.ts` 期望 75 → **75 passed**（无回归）；`notices-consistency.test.tsx`（658 行，untracked 新文件）
`grep -c "  it("` = 22，vitest 报数 22、退出码 0，非跳过/非空跑。

---

## 7. 接口归属与边界纪律

### 7-1 落点选择（原则 3 的取舍说明）

`assetNotices.ts` 放 `apps/canvas/src/` 与原则 3（公共**结果类型**放共享层）不冲突：原则 3 的约束对象是
「公共结果类型」，不是「用户可见文案」。规格 `acceptance-and-backlog.md:501` 把 P0-C 的文件归属写成
`apps/canvas/src/assetNotices.ts`；文案表**没有任何跨包消费者**（实测 `grep -rn "assetNotices" apps packages`
的命中全在 `apps/canvas`），符合原则 2「没有消费者的空实现不做」。依赖方向保持
`apps/canvas → packages/react` 单向前进，包层不反向依赖应用。本文件只 `import type`（`assetNotices.ts:36`）
与只读再导出（`:50`），**不定义任何被包层引用的类型**。

### 7-2 硬边界逐条

- **边界 1（不补救旧的无反馈操作）**：`FileManagerTree.tsx` 的行内动作**实测无 `disabled` / in-flight 守卫**
  （`grep -rn "disabled=" apps/canvas/src` 无该文件），本轮**不为它们新增**提示/禁用/错误处理；
  只统一**已有**模式的呈现。
- **边界 2（不新增操作入口）**：`git diff HEAD -- apps/canvas packages` 中
  `grep -E '^\+.*(onClick|onMouseDown|onPointerDown|onKeyDown|role="button"|<button)'` **零命中**
  （正反向均零）。交互面唯一改动是补传 `busy`，效果是**减少**可达调用。
- **边界 3（不改 P0-A/P0-B 判定逻辑）**：见 §1 哈希表——`fileOps.ts` / `assetStoreCopy.ts` /
  `AssetPanel.tsx` / `assetViews.tsx` 的 worktree hash-object 与 `HEAD:` **逐字相等**；
  `FileOpErrorCode` 八码域、`FILE_OP_ERROR_CODES`、`isRetryable`、`classifyFileOpError` 只读；
  `assetStoreCopy.ts` 六导出 + `isForbiddenAssetCopy` 只读；两域映射一律在 `assetNotices.ts` 层做。
- **边界 4 / P1-B**：`assetNotices.ts` 无 `lazy` / `import(` / 引用查询 / orphan 字样；未做懒加载/删除/
  引用查询/断图修复。

### 7-3 无「两套说法」独立复算

剥离注释后行内精确匹配（遍历 `apps/canvas/src` + `packages/react/src`）：
`正在处理上一步操作，请稍候再试（本次未做任何改动）。` = **1**（`assetNotices.ts:118`）；
`上一份写入还没有结束，请稍后重试（本次未做任何改动）。` = **1**；`操作失败，请重试。` = **1**；
`已在工作区 assets/ 保存了一份副本` = **1**；退役变体 `上传失败，请重试。` = **0**、
`文件已不在磁盘上。` = **0**。

---

## 8. 未覆盖清单（如实）与待主控裁定项

### 未覆盖（未运行 / 未验证，**不冒充**）

1. **容器一致化（范围 ②）未落地为新组件**：派单书指定的 `FileManagerChrome.tsx` 实测无提示条容器；
   真实容器分散于 `MindmapStage.tsx` / `FileOpPanels.tsx` / `FileManagerViews.tsx`。本轮仅登记差异与统一
   文案层的位置/停留语义，**未新建容器组件、未改三处取用方式**。见 §8 待裁定 a。
2. **`FileManagerTree.tsx` 的重复点击禁用**：实测无守卫（空缺），按边界 1 未新增。见 §8 待裁定 b。
3. **`FileManagerViews.tsx:173,193` 手写 style** 未对齐 `inlineBarStyle`（`fileManagerShared.ts:61-69`）。
4. **`unsupported-format` 运行时不可达**：`assetInsert.ts:53` 的 `reason` 把
   `'unsupported-format'` 窄化成 `'no-workspace'`（注释未说明原因）。既有键与语义只读，仅登记。
5. **`MindmapStage.tsx` 的其他就地拼接告警**（`:1544/:1640/:1669` 的 `切断未提交：` / `Section 未提交：`）
   按边界 1 未迁移，仅登记。
6. **`MindmapStage.tsx:2037` 的 `已保存到 ${doc.name}`** 与 `assetStoreCopy.ts:84 formatAssetWriteNotice`
   同前缀**不同语境**，**不合并**（符合「须判明归属，不得顺势合并」）。
7. **`AssetPanel.tsx` 无需改动**（实测）：该文件内**无任何文案副本**（
   `grep ASSET_STORE_BADGE|badgesFor|formatAssetStoreDetail` 零命中），徽章渲染在 `assetViews.tsx:14,25`
   经 `badgesFor(store)`，字节唯一来源是只读的 `assetStoreCopy.ts`；hash-object 与 HEAD **等于**（§1 表）。
8. **资产侧无生产消费方**：`ASSET_SESSION_REASON_COPY` / `uploadFailureNotice` /
   `ASSET_INSERT_REFUSAL_COPY` 的**导出面**已交付且被检查器断言，但除 `insertRefusalNotice` 外
   尚无真实调用点（接线属后续轮次）。故「同码同文案」用例从**导出面**断言，而非两处真实调用点。
9. **未运行，如实列出**：`pnpm gate:fast`、`pnpm build`、`node scripts/check-code-budget.mjs`、
   react / kernel 全量、`depcruise` 本会话均未运行（§4 表为执行记录，非本会话复跑）；
   NC-1 / NC-2 负控日志系读归档产物，**本会话未重跑**；§七 补验脚本本会话未重跑（读归档）。
10. **非 git 可比对性限制**：本工作树改动**未提交**（status 非空），故「上游字节未改」以
    `git hash-object` vs `HEAD:<path>`（§1）+ sha256 佐证；NC 的「改动前」基线哈希取自归档记录。
11. **F1-S2G 间歇拦截 / N1/N4 两缺陷 / A1 重扫停滞**：属产品缺陷，已上报（§5），未在本轮修复
    （不在 P0-C 范围）。
12. **flake**：kernel 全量 11 次中 2 次 flaky（各 1 failed），未定位根因。

### 待主控裁定（派单书 §四「额外需裁定边界」，本轮发现，**未自行扩围**）

- **a.** 容器一致化的落点：严格读法是「在 `FileManagerChrome.tsx` 新增公共容器定义 + 改三处取用」，
  但 `MindmapStage.tsx` 已 3150 行且 `bigFiles` 4/4 顶格，改动须近于净持平。
- **b.** 重复点击禁用落到 `FileManagerTree.tsx` 才有效，而该文件不在 §5.2 P0-C 文件归属列内，
  本轮不碰，交主控决定是否纳入。
- **c.** 本仓「禁用」三类语义（在途 / 能力 / 约束）只统一**呈现契约**，不得合并判定（边界 3）。

---

## 9. 提交列表（sha + stat）

**本轮在制品尚未提交**（`git status --porcelain` 非空，见 §1）。按派单书 §八-2「证据随提交入库」与
§八-5「回执提交在最后」，应提交的内容与规模如下（`git diff --stat` 实测，untracked 新文件另计）：

```
$ git diff --stat
 apps/canvas/src/FileManagerOpStack.tsx           |   8 ++
 apps/canvas/src/MindmapStage.tsx                 |  63 +++++-----
 apps/canvas/src/SidePanels.tsx                   |  25 +++-
 apps/canvas/src/currentDocOps.ts                 |   8 ++
 apps/canvas/src/hooks/useCurrentDocDeleteFlow.ts |  23 +++-
 apps/canvas/src/hooks/useFileOpOrchestration.ts  |  50 ++++----
 apps/canvas/src/useFileOpController.ts           | 110 +++++++++++++++-
 apps/canvas/tests/no-native-dialogs.test.ts      | 152 +++++++++++++++++++++++
 package.json                                     |   2 +-
 pnpm-lock.yaml                                   |  32 ++---
 10 files changed, 382 insertions(+), 91 deletions(-)
```

| 类别 | 文件 | 规模 | 基线处 sha |
|---|---|---|---|
| 新增（untracked） | `apps/canvas/src/assetNotices.ts` | 319 行 / 18461 B | 无（新文件） |
| 新增（untracked） | `apps/canvas/tests/notices-consistency.test.tsx` | 658 行 / 33571 B | 无（新文件） |
| 改动 | 上述 10 个 tracked 文件 | `+382/-91` | 见 §1 血缘说明 |
| 补验脚本（untracked） | `tools/verify-a1-workspace-switch.mjs`、`verify-n1n4-reopen.mjs`、`verify-f1f2-browser.mjs`、`verify-no-native-dialogs.mjs`、`tools/lib/browserHarness.mjs` | 5 文件 | 无 |
| 证据（untracked） | `docs/dispatch/evidence/2026-09-23-p0c-negative-controls/`（4 文件）、`…/p0c-e2e/`（7 文件） | 深 | 无 |
| 未入库文档（untracked，**非本轮产物**） | `docs/dispatch/2026-09-23-P0-B-review.md`、`docs/dispatch/2026-09-23-P0-C-dispatch.md` | — | 无 |

**提交粒度建议**（每个逻辑项单独提交，message 说清「为什么」）：① 文案事实源 + 迁移；
② 禁用判据接线；③ 守卫扩展 + 一致性用例；④ 负控证据；⑤ §七 补验脚本与证据；⑥ 本回执。
**本轮未执行提交**（回执即交付物，由上层按 §八-2/§八-5 次序入库）。

---

## 10. 纪律自检（§八 逐条）

| §八 | 条 | 本轮回执的满足方式 |
|---|---|---|
| 1 | 基准防漂 | §1 开工/收工各锚一次；三锚（HEAD / `:packages` / `:apps`）开工逐字等于期望；收工以**产品树锚 + 血缘**为准并说明（DS-10 / P0-B 同款） |
| 2 | 证据随提交入库 | 负控/补验/回执证据目录已就位（§9）；**尚未提交**，如实在 §1 与 §9 申报 |
| 3 | 派单书已入库 | `docs/dispatch/2026-09-23-P0-C-dispatch.md` 存在；**实测为 untracked**（与「已入库」不符，如实申报）；冲突以规格原文为准 |
| 4 | macOS bash 3.2 / ${var}、Python 非 ASCII | 本文档与命令均未使用危险拼接；归档日志为逐条实跑产物 |
| 5 | 提交粒度 | §9 给出按逻辑项的提交建议；回执放最后 |
| 6 | 未运行项如实列 | §8 未覆盖清单逐条列出（含未跑门禁、未重跑负控、未跑补验脚本）；M 层人工项**未标 satisfied**（F1③/F2 的 unconfirmed 如实） |
| 7 | 停止点 | 本回执即 P0-C 收口；**不自动进入 P1**；不越界施工（§7-2 边界 1–4 逐条） |
| §三 | 归属纪律 | §7-1 说明 `assetNotices.ts` 放应用层的理由；`packages/react` 不改（§1 哈希）；不硬编码两套说法（§7-3 复算） |

---

**回执路径**：`docs/dispatch/2026-09-23-P0-C-report.md`
