# G+H 混合批（日常闭环 + 质量纵深）执行计划

> 日期：2026-08-29 · 阶段判断：项目近功能饱和，本轮重心 = 日常可用性 + 边界修复 + 真实数据验证
> 范围：G1 自动保存 / G2 SVG 导出 / G3 拖文件打开 / H1 折叠定位自动展开 / H2 真实 10K 大图验收

## 任务

- [ ] **T1 拖 .mm.md 到窗口打开**（G3）：复用 MapView onAssetFiles drop 管线——Stage 分支：非图片且 .mm.md/.md → 打开文档（applyDoc），图片 → 上传资产
- [ ] **T2 折叠定位自动展开**（H1）：focusNode/关系面板定位目标在折叠子树内 → 自动展开祖先（expandPathTo 纯函数 + Stage 接线）——F1 已知边界
- [ ] **T3 自动保存**（G1）：dirty 后 debounce 300ms 写回句柄（新建未保存跳过；手动 Ctrl+S 立即执行并取消 pending；失败静默）
- [ ] **T4 SVG 导出**（G2）：exportSvg 纯函数（当前视图可见节点/连线 → SVG 字符串，复用 backend 场景原语）+ 导出按钮下载
- [ ] **T5 真实 10K 大图验收**（H2）：构造 10K 节点 .mm → 打开/折叠/编辑/保存全流程实测，数据入报告——**本批硬门禁**

## 验收（:5174）

1. 拖 .mm.md 到窗口 → 直接打开该文档；拖图片 → 仍走上传
2. 折叠某分支后从关系面板/大纲点子树内节点 → 自动展开祖先并定位
3. 编辑后 300ms 自动写回（有句柄时）；Ctrl+S 立即保存
4. 「导出」→ 下载当前视图 SVG（可见节点/连线齐全）
5. 10K 节点 .mm 全流程可用，数据落档

## 冻结纪律

全 minor：新文件（expandPathTo/exportSvg/debounce 工具）+ Stage 接线；MapView 无签名变化（onAssetFiles 已透传）。
