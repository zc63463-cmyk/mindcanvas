# 第 4-R 步 · P0-D 独立复核重跑 · 外派执行计划 + 启动 Prompt

- 日期：2026-09-23
- 性质：**独立复核（只读，不修代码）**
- 复核目标：P0-D 候选，分支 `main`，HEAD `a3a0c0c792a03855313d9acad0f40157c1df17e3`
- 派单方已完成的预核：见 §1.2

---

## 1. 为什么必须重跑

### 1.1 上一次复核的基准错位（已由派单方实测确认）

上一轮回执（`bbf7464` 号回执）判 P0-D「supports」，但并列出了 3 条 high + 12 条 medium finding。派单方逐条实读后确认：**这些 finding 描述的问题在当前盘上已不存在**，原因是复核读到的代码早于修复提交。

实测的提交时序：

| 时间 | 提交 | 内容 |
|---|---|---|
| 09-22 21:50 | `046bccd` | 交付报告（round 1） |
| 09-22 22:58 | **`37f1bfa`** | fix: **M7 用真实 kind 前缀（img/draw）判归属**、**M6 精确命中须同作用域+有证据**、修投影删掉未迁移旧键 |
| 09-22 23:26 | `c9c0828` | fix: savedAt 接生产保存路径、移除直写旧库的第二写路径、relink 改为用户显式选目标、投影失败可查 |
| 09-22 23:53 | **`24fc252`** | fix: **M6 的 docKey 命中分支也须属当前作用域**（消除跨作用域错误认领） |
| 09-23 01:11 | `85b23b2` | fix: M5/M6/M7 归属证据严格化——旧库 id 只是文件名，不凭空造 ws 身份 |
| 09-23 01:14 | `ec16934` / `d320c03` / `ca19873` / `df691bc` | 接线层、savedAt、第 3 轮守护 |
| 09-23 01:16 | `bbf7464` | 第 3 轮回执 |
| 09-23 01:57 | `a3a0c0c` | 报告修订 |

**报告自己的 commit 表只覆盖到 round-1 的 `9af7d0e`，门禁节写「HEAD = `24fc252`」** —— 即复核读的是修复前状态，且未在复核结束时重新 `git rev-parse HEAD`。

### 1.2 派单方已逐条预核的结论（供复核者参考，**不是让你照抄**）

| 上一轮 finding | 声称位置 | 派单方实读结论 |
|---|---|---|
| M7 磁盘资产永不可迁移，猜 `disk:` 字面量 | `docIndexMigrate.ts:229-230` | **不成立**：`parseAssetFavKey`（`:436`）从键切出真实 `kind`，校验 `img`/`draw`；全仓无 `disk:` 字面量 |
| 工作区文档 scopeId 恒 null | `useIndexWiring.ts:88` | **不成立**：`:88` 为 `useWorkspace && workspace?.scopeId ? workspace.scopeId : null`，`:89` 另有 `workspaceScopePersisted` |
| 投影永久抹掉 source | `docIndexProject.ts:36-96` | **不成立**：`:95` 有 `replaceAll([])` 会抹空的守卫；`:102` 写失败返回 false → 置 `projectionFailed` |
| M6 跨作用域认领 | `docIndexMigrate.ts:155-178` | **不成立**：`:168-178` 注释逐条列出作用域/ephemeral/证据三条件 |
| M7 跨作用域复用 | `docIndexMigrate.ts:227-239` | **不成立**：`:248-249` `ownScope` 显式限定作用域 |
| `projectionStatus()` 无生产读取方 | — | **不成立**：`FileManagerViews.tsx:170` 即生产读取方 |

派单方同时实测：`apps/canvas/tests/doc-index.test.ts` 51 passed、canvas 全量 49 files/456 tests exit 0、`gate:fast` exit 0、`build` exit 0。

**但派单方没有做的事**：没有逐条验证上一轮那 12 条 medium / 9 条 low，也没有对当前 revision 做过任何独立复核。**这正是本次派单的目的。**

### 1.3 两个**未解决**的真实问题（本次复核必须覆盖）

1. **契约缺口（已确认）**：`docs/specs/2026-09-19-file-assets-design/shared-contracts.md:857` 只写 `mindcanvas.assets.fav` 的键形如 `kind:id`，**未定义 `kind` 的取值域**。实现自行解析为 `img`/`draw` 是对的，但契约缺这一句。P0-B / P1-B 会撞同一个键。
2. **「两库互相喂大」疑点（派单方未复现，**必须由你独立判定**）**：投影把相对路径写进旧库 → 下次 M5 把每行当真实文档 → `legacyKeys` 只增不减 → 两个存储每次会话互相增长。相关位置 `docIndexProject.ts:139-144,164-166,89-95`、`docIndexMigrate.ts:130,192,264`。**这是上一轮回执自己提到但未定性的项，比它列出的多条 high 更接近真实缺陷。**

---

## 2. 启动 Prompt（复制即用）

> **交付纪律（给派单方，不属 prompt 正文）**：本派单书目前是 **untracked** 状态
> （与另三份裁定书一同未提交）。若复核者 clone 干净树或切到其他工作树，
> **本文件不会随之出现**。派单前二选一：
> ① 先提交本文件（及其引用的四份 untracked 文档）；或
> ② 把下方 prompt 全文**随派单消息一并投递**，不让复核者依赖仓库里的这份文件。
> 上面 §一/§三/§四 的基准值、行号与预核表都已在 prompt 正文内自带，故 ② 不会丢失判据。

```text
你是 MindCanvas P0-D 的**独立复核执行者**。你的任务不是实现、不是修改，而是对当前候选
做一次**从证据出发的证伪式复核**。你做完全部判定后停下，交回回执。

仓库：/Users/xin020/Desktop/temp/workbase/mindcanvas-portable-20260921/mindcanvas-s5
（注意：外层目录 mindcanvas-portable-20260921 只是便携打包目录，**不是 git 仓库**；
真正的仓库是上面这个子目录，分支 main。）

════════ 一、基准锚定（第一步就做，做完写进回执） ════════

上一次复核失败的根本原因是**基准过期**：它读到的是修复前的代码，却以为在评当前候选。
你必须先用下面三条命令把基准钉死，并把输出原文抄进回执：

  git rev-parse HEAD
  git status --porcelain          # 应为空（派单方实测：仅 4 个 untracked docs 文件）
  git status --porcelain -- apps packages    # **必须为空** —— 产品树无未提交改动的书面证据
  git log --oneline -6

**本次复核目标 HEAD = a3a0c0c792a03855313d9acad0f40157c1df17e3**
（若实测不符，停止并报告 —— 不要用一个与你实测不同的基准开工）

`git status --porcelain -- apps packages` 为空是**开工前置条件**，不是顺带一提：
上一次复核的失败根因就是「基准与代码不一致」，所以「无产品改动」必须**被验证**而非被假定。
若该命令有输出，停止并报告 —— 你评的将是一个与候选不同的树。

**防漂要求**：这个工作树上有并行活动，HEAD 可能在复核期间前进（历史上已连续发生两次）。
因此：
- 在回执抬头写「复核开始时的 HEAD」与「复核结束时的 HEAD」，两者不一致要显式说明。
- 本步的**产品树锚**（不受 docs-only 提交影响，可作为稳定判据）：
    git rev-parse HEAD:packages   → 期望 c771dd3bc061b917a195ecf03ac73129c431df71
    git rev-parse HEAD:apps       → 期望 0ce15a9304db5fdfd7288dd8d5f9c540ec233f53
  若产品树锚与你实测不符，**停止并报告**，不要在错的基准上给结论。
- 你**不得**修改任何被审文件（详见 §六）。

════════ 二、复核对象 ════════

P0-D 候选的产出文件（逐个实读，不要只看报告）：
  apps/canvas/src/docIndex.ts          （22.6 KB，唯一索引写入口）
  apps/canvas/src/docIndexCore.ts      （16.5 KB，类型与纯函数）
  apps/canvas/src/docIndexMigrate.ts   （19.5 KB，M4–M9 + M11 迁移与归属证据）
  apps/canvas/src/docIndexProject.ts   （ 7.1 KB，降级投影 R-A/R-B）
  apps/canvas/src/useIndexWiring.ts    （ 5.1 KB，接线层）
  apps/canvas/src/FileManagerViews.tsx  （ 7.6 KB，历史池呈现）
  apps/canvas/tests/doc-index.test.ts  （47.2 KB，51 用例）
  docs/dispatch/2026-09-22-P0-D-report.md （57.4 KB，施工方交付报告）

权威契约（判据来源，必须实读原文，不要采信报告对它们的转述）：
  docs/specs/2026-09-19-file-assets-design/shared-contracts.md
    —— §4.5.4 AssetIndexEntry、§4.7 DocIndexEntry
    —— §6 迁移表 M1–M11（本包只做 M4–M9 + M11）
    —— §6.2.1 归属证据规则（**判据核心**）
    —— §6.3 版本回退策略（R-A / R-B / 降级投影）
  docs/specs/2026-09-19-file-assets-design/acceptance-and-backlog.md 的 P0-D 段
    （范围 / 排除 / 文件归属 / 测试 / 四条负控 / 停止点）
  docs/specs/2026-09-19-file-assets-contract-close/p0-0-implementation-plan.md §5
    （阴性对照的写法权威）
  docs/dispatch/2026-09-20-file-assets-foundation-dispatch.md §3（共同交付契约 12 条）

════════ 三、已知的基准错位（你必须重新判定，不得照抄） ════════

上一轮回执（HEAD 写 `bbf7464`，但实际读的是 `046bccd`~`24fc252` 之间的状态）列了
3 high + 12 medium + 9 low。派单方已实读并认为其中至少 6 条在当前盘上不成立，
原因是**修复提交 `37f1bfa`(22:58) / `c9c0828`(23:26) / `24fc252`(23:53) 早于回执但晚于复核读取**。

派单方给出的预核结论见下表。**这是线索，不是结论** —— 你要么独立复现它、要么独立推翻它，
两种结果都要给出你自己的证据（命令 + 真实退出码，或 path:line）：

| 上一轮 finding | 派单方预核 | 你要做的 |
|---|---|---|
| M7 猜 `disk:` 字面量 → 磁盘资产永不可迁 | 不成立（`parseAssetFavKey` 解析真实 kind） | 自己读 `parseAssetFavKey` 与 M7 分支，独立判定 |
| 工作区文档 scopeId 恒 null | 不成立（`useIndexWiring.ts:88` 已判空 + `workspaceScopePersisted`） | 追完整条接线链，看 scopeId 从哪来、何时为 null |
| 投影永久抹 source 不上报 | 不成立（`docIndexProject.ts:95` 有守卫、`:102` 置失败） | 重点验**失败是否真的可查**，以及 quota 路径 |
| M6 跨作用域认领 | 不成立（`:168-178` 三条件在位） | 自己构造跨作用域夹具复现或证伪 |
| M7 跨作用域复用 | 不成立（`:248-249` `ownScope`） | 同上 |
| `projectionStatus()` 无生产读取方 | 不成立（`FileManagerViews.tsx:170`） | 确认该读取方是否真的消费、还是仅调用后丢弃 |

**你尤其要警惕这两类错误**（上一轮的两方都犯过）：
- 把「文档写了」当成「代码实现了」；
- 把「测试文件存在」当成「断言覆盖了该守点」。
- 反之也要警惕：把「我读到的行号处没有该代码」当成「该问题不存在」——
  **先确认你读的是当前基准，再确认行号是否已因其他提交而漂移**。

════════ 四、必须独立判定的三个未决问题 ════════

**问题 1（契约缺口，已确认一半）**：
`shared-contracts.md:857` 是否真的未定义 `mindcanvas.assets.fav` 的 `kind` 取值域？
实读该行及其上下文，给出原文与你的判定。若确认缺失，说明它对 P0-B / P1-B 的影响。

**问题 1.5（本步的重点要求）：「守卫在位」是否等于「守卫有效」？**

派单方预核的六条里，至少两条给出的证据形态是「**守卫在盘**」：
- `docIndexProject.ts:95` 的 `replaceAll([])` 守卫
- M6 `:168-178` 的作用域 / ephemeral / 证据三条件
- M7 `:248-249` 的 `ownScope`

**「守卫存在」与「守卫被判别性负控钉住」是两件事。** 本仓已有前例：
上一轮 NC-3 中性化 `scopeIdentity.ts:155` 后**两个套件仍全绿** ——
即那条守卫存在、但没有任何测试能区分它与它的缺失。

因此你必须对 **M6 / M7 的作用域条件**分别回答：
- 现在有没有一条负控，**在中性化该条件后真的转红**（给出中性化内容、转红用例原文、真实退出码）？
- 若没有：明确写「该守卫未被钉住」，并给出**能被钉住的最小负控设计**（不必实现，给出方案即可）。
- 你的结论必须让主控能回答：「如果将来有人删掉这个条件，哪个测试会拦住他？」

**问题 2（派单方未复现，必须由你定性）：「两库互相喂大」**

疑点链路：投影把相对路径写进旧库（`library.v1`）→ 下次 M5 把每行当真实文档读入
→ 生成新索引条目 → `legacyKeys` 只追加不删除 → 于是两个存储每次会话都互相增长。

**派单方已确认该链路的代码在 HEAD 上存在**（实读）：
- `docIndexProject.ts:50-54` 收集 `opaqueRows`（无法表达的畸形行）
- `:91-92` 把它们**原样附着在末尾**（注释自陈「确保『不处理也不丢』」）
- `:82-89` 旧库中索引没有对应项的条目**原样保留**（只增不减）
- `:111,128` 明确**拒绝移除尚未迁移的旧键**（注释自陈「否则投影反过来破坏迁移的输入」）
- 故 `projectLibrary` 的行集合是**并集语义**：`index.docs` ∪ 旧库行 ∪ 畸形行

所以「不丢数据」与「单向增长」是**同一个设计选择的两面**。这是有意为之，不是疏忽——
但它确实意味着**只有删除旧键才会终止增长**。

要求：
- 用**受控夹具**实跑复现或证伪。给出可测的量级：**每会话净增几行？是否有上限？**
  （注意：若索引条目与旧库行在稳态下收敛，那就是「有界噪音」而非缺陷——请明确区分。）
- 判定它是**真缺陷** / **有界噪音** / **派单方读错了**（三种都算有效结论）。
- 若用临时探针脚本验证，**用完必须还原**，并在回执中说明还原校验方式。

**问题 2.5：你的问题 2 结论是 DS-10 的必需输入，不是平行轨道**

DS-10（旧键删除时序）与增长循环是**同一个决策的两面**：增长循环的唯一终止方式就是删旧键，
而删旧键正是 DS-10 在问的事。因此：
- 你的问题 2 结论**直接决定** DS-10 的选项空间（若增长无界，则 DS-10 从「可选清理」变成「必须处置」）。
- 回执中必须单列一节，标题明确为「**对 DS-10 的输入**」，让主控能据此决策。
- **但你不做 DS-10 的裁定**——那是主控的。你只提供判据。

════════ 五、复核方法与判定要求 ════════

**1. 逐条重跑四条阴性对照**（acceptance-and-backlog 的 P0-D 负控栏）：
  ① 旧键损坏 → 索引仍可用且不崩
  ② 同一旧键迁移两次 → 结果一致（幂等）
  ③ 某旧键在当前目录**唯一同名命中** → **不自动绑定**且进历史池
  ④ 升级后新增收藏 → **降级投影里能看到**（证明回退不是「旧键还在」而已）

对每条回答四个问题（p0-0-implementation-plan.md §5 的纪律）：
  中性化了哪块守卫 / 正确断言是否保持不变 / 预计哪条用例转红 / **转红时命令是否真的退出非零**。
**退出码一律重定向后取 `$?`，不要用管道后的值。**
**中性化必须先在盘确认**（grep 计数符合期望）再跑红跑；未落盘的变异不得计为「已证明转红」。

**2. 门禁逐数复算**（不要采信报告的读数）：
  pnpm gate:fast / pnpm build / node scripts/check-code-budget.mjs
  以及 lint 的 warnings/infos 逐数（本仓冻结基线：lint 1541 warnings + 48 infos）
  **注意**：派单方实测 build 后 lint 计数会变化（dist 在 biome 扫描范围内）。
  若你复算数字与报告不符，如实报告并给出你的命令与输出。

**3. 特别复核上一轮那条无法复现的断言**：
上一轮称 `mode-guard.test.tsx` 约 50% flaky（5/10 红）、全量 canvas 48 passed/1 failed。
派单方实测：canvas 全量 49 files / 456 tests 全绿 exit 0。请你实跑多轮并给出你自己的读数。
若确为全绿，说明上一轮那个数字为何产生（是否也是基准错位造成的）。

**4. 判定「证据强度」而非只判「对错」**：
每条结论标注它是：
  - `verified`：你亲自跑/亲自读，给出命令 + 真实退出码或 path:line；
  - `unconfirmed`：你复现不出来，说清你试了什么、卡在哪里；
  - `refuted`：你发现原主张有误，给出反证。

════════ 六、硬性纪律 ════════

1. **只读**。不修改、不暂存、不提交、不推送任何被审文件。
2. 若因复核必须临时改文件（例如中性化负控），**必须自备备份、字节级还原、
   用 `cmp` 校验还原一致**，并让终态 `git status --porcelain` 回到你开工时的状态。
   回执中必须记录还原校验方式。
3. 你**不得**采信施工方回执的结论性表述；每条以**你自己的实跑/实读**为准。
4. 不得为了「让结论好看」而放宽判据；也不得为「显得有产出」而把已修好的项硬列成缺陷。
5. 不引入 `it.fails`、反转断言、`catch` 后报 PASS、宽泛 `skip`。
6. 真实选择器手势、真实 OS 输入法、跨浏览器、触控、真浏览器矩阵 —— 本机不具备条件
   （playwright 未安装），**如实标注 unverifiable 并说清缺什么**，不得冒充已验证。
   注意：`tools/` 下共 18 个 `verify-*.mjs`。派单方最初写「11 个命中 playwright」，
   自查后更正为 **18 个全部命中**（`grep -l playwright tools/verify-*.mjs | wc -l` → 18）——
   所以「全部 verify 脚本都不可运行」这个说法本身成立，但**你自己数一遍再写**，
   别照抄派单方（这条我第一稿就写错了）。
7. 做完即停，**不启动 P0-A**，不给出「下一步该做什么」以外的自行推进。

════════ 七、交付 ════════

交付 docs/dispatch/<日期>-P0-D-review.md，含：

1. **基准锚定表**：复核开始/结束的 HEAD、工作树状态、两个产品树锚（packages/apps）
2. **逐条复核表**：上一轮 3 high + 12 medium 每一条 → 你独立测得的结论
   （成立 / 不成立 / 无法复现）+ 你的证据。**不采信派单方预核，单独给证据。**
3. **四条阴性对照实测**：中性化内容、保持不变的期望、转红用例原文、真实退出码、还原校验
4. **三个未决问题**的独立判定：
   - 问题 1：契约缺口（`mindcanvas.assets.fav` 的 kind 域）
   - 问题 1.5：**守卫有效性** —— M6 / M7 的作用域条件是否被判别性负控钉住；
     未被钉住的，给出最小负控设计
   - 问题 2：增长循环的复现或证伪（含量级：每会话净增几行、是否有上限）
5. **「对 DS-10 的输入」单列一节**：你的问题 2 结论如何约束旧键删除时序的选项空间
6. **门禁逐数复算**：命令 + 真实退出码 + 与冻结基线对照
7. **本轮新发现**（若有）：按 severity 分级，每条带 path:line 与证据
8. **未核验声明**：如实列出你未核验的项（不得含糊）
9. **最终判定**：ACCEPT / REJECT / ACCEPT-with-conditions，并说明每条判据
10. **边界声明**：未 push / 未合并 / 未修改被审文件；临时改动已还原并校验

**报告纪律**：
- 数字贴工具原样输出，不四舍五入美化。
- 不把 unverifiable 包装成 satisfied；不把「文档写了」当「代码实现了」。
- 判定权在你，责任也在你；施工方回执的结论性表述不作依据。
```

---

## 3. 派单方对本次复核的预期与边界

### 3.1 三种可能的结论，都算成功

| 结论 | 含义 | 派单方后续动作 |
|---|---|---|
| **ACCEPT** | 上一轮 finding 确为基准错位，当前 revision 经独立复核通过 | 开 P0-A |
| **ACCEPT-with-conditions** | 主体通过，但「两库互相喂大」或新发现需处置 | 按条件处置后再开 P0-A |
| **REJECT** | 存在真实 high 缺陷 | 返修，再复核 |

**我不预设结论。** 我给出的六条预核只是线索，唯一目的是避免复核者重走「读到旧基准」的弯路。若你复现出与我相反的结论，那正是本次派单的价值所在。

### 3.2 本步不做的事

- 不修任何代码（包括我怀疑的「两库互相喂大」—— 先定性，修不修由主控定）
- 不启动 P0-A
- 不处理 DS-10（旧键删除时序，仍是主控决定）

### 3.3 派单方如实声明

1. **我实测确认了基准错位**（提交时间戳 + 报告自称的 commit 表覆盖范围），这是本次派单的直接依据。
2. **我对六条 finding 的预核是实读源码所得**，但我**没有**逐条验证上一轮那 12 条 medium 与 9 条 low，也**没有**对当前 revision 做过完整独立复核 —— 那正是要派给你的。
3. **「两库互相喂大」我未复现**，只有链路推断。它可能是真的，也可能是我读错了；prompt 里明确要求你独立定性。
4. **我不知道上一轮复核者为什么读到旧基准**。可能是它开工时 HEAD 就在那儿（`046bccd` 是 21:50，复核很可能在 22:58 的修复之前就开始跑了），也可能是中途没有重新锚定。这个成因不影响本次派单，但提醒一件事：**本工作树的 HEAD 会漂，任何复核都必须首尾各锚一次。**
