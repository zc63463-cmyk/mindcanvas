# FILE-ASSETS-DESIGN 外派启动 Prompt

你是 MindCanvas 下一阶段“文件管理与图库增强”的设计负责人。请完成设计交付，不进入功能实现。

工作区：`<workspace>`。

先完整阅读：

1. `docs/dispatch/2026-09-19-file-assets-design-dispatch.md`，这是本包范围与交付标准。
2. `docs/dispatch/2026-09-19-delivery-close-final-acceptance.md`，这是已经结束的上一轮验收边界。

请把已有功能与新增设计分清：文件树、搜索、最近、收藏、基本目录操作，以及图库网格/列表、搜索、上传、粘贴与插入都已有实现。不要重新包装成从零开发，也不要只画美化后的界面。沿真实调用链核实导图与自由画布各自的入口和限制。

按顺序完成：

- D0：现状矩阵、源码锚点、共享身份与状态机，比较至少三种架构方案并推荐渐进路线。分清文档会话、保存目的地、句柄、文件路径、工作区、资产身份、序列化引用和临时 URL。
- D1：文件管理体验与一致性设计，重点是当前文档改名/移动/删除、在途保存、复制成功但删除失败、失联重新定位、最近/收藏同步和嵌套目录资产引用。
- D2：图库发现与预览、同名策略、资产归属/引用/替换/删除/恢复、浏览器与磁盘的真实持久化反馈、跨工作区缓存与迟到结果隔离。区分移除节点引用与删除实际文件。
- D3：旧数据兼容与迁移、两种画布的联合旅程、可反证的验收矩阵，以及 P0 可靠性→P1 日常工作流→P2 可携带性的独立开发包。

按任务书在独占目录交付 README、现状、共享契约、文件设计、图库设计、验收与开发拆包、设计回执共七份文档。提供文字线框、具体提示文案、接口草案与关键状态图；每个核心操作覆盖成功/取消/失败/部分成功。清楚标注已验证事实、风险线索和设计提案，不将建议写成已实现。

本包可自主读代码、调查、写新设计文档；普通设计选择给出推荐和理由后继续，不反复请求确认。不得修改产品源码、测试、门禁和验收工具，不迁移真实数据，不动既有证据/清单，不提交/暂存/推送/发布，不运行破坏性 Git 操作，不启动下一开发包或另建执行任务。工作树未提交成果属于已验收候选的一部分，不能仅从 HEAD 重建当前状态。

不重新打开 DELIVERY-CLOSE；引用历史验证时标明来源。本包不要求重跑全仓门禁。需要新证据时，仅使用独占夹具和输出，不修改正确期望。不要将系统选择器替身等同真实手势，也不要承诺浏览器文件删除有系统回收站或单文件天然携带本地图库。

提交设计回执前自行核对：路径有效、能力现状有源码依据、共享契约前后一致、核心未知项有建议与调查办法、开发包依赖清楚、验收能识别典型错误。汇报主要方案、交付路径、实施前少数决策与未做项，状态写为“设计候选可复核”，然后停止，等待主控评审。
